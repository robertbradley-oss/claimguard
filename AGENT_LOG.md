# ClaimGuard Agent Log

This file is for notes from autonomous project-agent runs and direct `/claimguardagent` tasks.

The agent should append short entries here when it performs repo work from `AGENT_INBOX.md` or a direct `/claimguardagent` request.

- 2026-05-31: Phase 3.3 local Case Review Command Center hierarchy polish. Updated `src/components/CaseReviewCommandCenter.tsx` so `/case-command-center` follows the current ClaimGuard off-white/parchment evidence workspace direction instead of a dominant dark forensic dashboard: stronger selected-evidence bench, dotted inspection surface, clearer metadata tiles, improved evidence sidebar scanability, selective charcoal review-signal/summary panels, clearer notes/manual decision placement, separate customer-safe wording, and a lighter timeline. Tightened `scripts/check-report-semantics.mjs` for the Phase 3.3 shell label, selected-evidence bench, and off-white route direction. Updated `ROADMAP.md`, `NEXT_STEPS.md`, and `REPO_SOURCE_OF_TRUTH.md` to record Phase 3.3 scope and the updated visual direction. The route remains static/local, synthetic-only, and unlinked; no mock data shape change; no `ClaimReviewWorkflow` edit, unsupported-evidence runtime wiring, ProductPhotoReviewPanel routing, live unsupported-evidence UI, persistence, upload behavior, providers/integrations, OCR/metadata/image analysis, `analyzeEvidenceFile` change, `LocalAnalysisResult` migration, receipt behavior change, deployment, or Phase 3.4 work was added.
- 2026-05-31: Phase 3.2 docs-only source-of-truth closeout alignment after pushed commit `a169de4` (`feat: add phase 3.2 local case command center shell`) and the post-push review/QA checkpoint. Updated status docs to mark Phase 3.2 pushed, reviewed, and safe to build on; record that `/case-command-center` renders as a static/local, synthetic-only, unlinked shell; record that `/` remains the existing Phase 1 receipt workflow and does not link to `/case-command-center`; record no browser console warnings/errors, no deployment, unchanged receipt behavior, unchanged `ClaimReviewWorkflow`, `ProductPhotoReviewPanel` still unrouted, unsupported/product-photo runtime still non-live, and no persistence/upload/integrations/OCR/metadata/storage/providers/auth/billing/real evidence. Recommended Phase 3.3 as visual hierarchy and selected-evidence structure polish only, still mock/local and synthetic-only. Docs-only; no runtime code, UI/component/route files, upload flow, report mapping, analyzer/parser/scoring/fixture files, ProductPhotoReviewPanel, ClaimReviewWorkflow, providers/storage/integrations/case queues, OCR/metadata, package/config/public/deployment files, commit, push, or deployment work was performed.
- 2026-05-31: Phase 3.2 non-persistent local Case Review Command Center shell implementation. Added `/case-command-center` as an unlinked local route, `src/components/CaseReviewCommandCenter.tsx` as the dark forensic case workflow shell, and `src/lib/case-command-center/mock-case.ts` as isolated synthetic mock case data. The shell includes a case header/status strip, evidence sidebar, selected evidence panel, case-level review summary, notes/manual decision placeholder, customer-safe wording panel, and timeline placeholder. Added semantic/privacy/import guard coverage in `scripts/check-report-semantics.mjs` and updated source-of-truth docs. Mock/local data only; no `ClaimReviewWorkflow` edit, unsupported-evidence runtime wiring, ProductPhotoReviewPanel routing, live unsupported-evidence UI, database/storage/auth/orgs/billing, providers/integrations, OCR/metadata/image analysis, `analyzeEvidenceFile` change, `LocalAnalysisResult` migration, receipt behavior change, deployment, commit, or push work was performed.
- 2026-05-31: Phase 3.1 planning/design-only Case Review Command Center concept. Added `PHASE_3_1_CASE_WORKFLOW_DESIGN_CONCEPT.md` and updated `DESIGN.md` plus status/source-of-truth docs to define the future dark forensic enterprise evidence-review visual direction, case workflow layout, planned design-system primitives, interaction model, safety/wording rules, technical boundaries, and recommended Phase 3.2 first-build target. The plan keeps Phase 3 implementation blocked until explicit approval and recommends a non-persistent local case shell with mock/local case data only as the first later build. Docs/design-only; no routes, components, runtime wiring, `ClaimReviewWorkflow` edit, live unsupported-evidence UI, ProductPhotoReviewPanel routing, database/storage/auth/orgs/billing, providers/integrations, OCR/metadata/image analysis, `analyzeEvidenceFile` change, `LocalAnalysisResult` migration, receipt behavior change, deployment, commit, or push work was performed.
- 2026-05-31: Phase 3.0 planning-only case workflow readiness. Added `PHASE_3_0_CASE_WORKFLOW_READINESS_PLAN.md` and updated status/source-of-truth docs to define the case object concept, planning-level evidence model, UX architecture, safety boundaries, technical boundaries, Phase 1 receipt inheritance, Phase 2 unsupported/product-photo readiness inheritance, first build recommendation, and recommended Phase 3 sequence. The plan chooses Phase 3.1 design system/case workflow concept planning as the next safe task before any implementation, and identifies the first later build target as a non-persistent local case review shell only after explicit approval. Docs-only; no routes, components, runtime wiring, live unsupported-evidence UI, ProductPhotoReviewPanel routing, database/storage/auth/orgs/billing, integrations, OCR/metadata expansion, `analyzeEvidenceFile` change, `LocalAnalysisResult` migration, receipt behavior change, deployment, commit, or push work was performed.
- 2026-05-31: Final Phase 2 closure source-of-truth alignment after the Phase 2.9.1 review-only closeout passed. Updated status docs to mark Phase 2 closed as non-live unsupported/product-photo readiness, record pushed commit `3779ab0` (`docs: close phase 2 non-live readiness`), record no deployment, preserve unchanged receipt behavior, keep live opt-in blocked, keep the dev-only unsupported-evidence bridge synthetic-only and production-blocked, keep `ClaimReviewWorkflow` unwired, keep unsupported-evidence live UI absent, keep `ProductPhotoReviewPanel` unrouted, keep `analyzeEvidenceFile` as the receipt analyzer entrypoint, keep `LocalAnalysisResult` receipt-shaped, and set the next recommended task to Phase 3.0 planning-only case workflow readiness. Docs-only; no runtime code, UI, upload flow, report mapping, parser, scoring, fixtures, providers/storage/integrations/case queues, OCR/metadata, package/config/public/deployment files, receipt behavior, Phase 3 implementation, commit, push, or deployment work was performed.
- 2026-05-31: Phase 2.9.0 docs-only unsupported-evidence live-opt-in readiness planning. Added `PHASE_2_9_0_UNSUPPORTED_EVIDENCE_LIVE_OPT_IN_READINESS_PLAN.md` and updated status docs to decide that unsupported evidence is ready for planning-only handoff, not live opt-in implementation. The plan records blockers (`ClaimReviewWorkflow` unwired, no live unsupported-evidence UI, `LocalAnalysisResult` receipt-shaped, `analyzeEvidenceFile` receipt-only, ProductPhotoReviewPanel unrouted, no real product-photo/OCR/metadata/provider/storage/integration/case-queue work, and no case workflow/audit/persistence foundation), defines the narrow future default-off workflow/caller boundary path, keeps dev bridge/dev cases dev-only, keeps mapper/helper/wrapper probe-only or non-live, lists browser/manual QA and semantic/privacy prerequisites before live UI, states what Phase 3 should inherit, and recommends one final review-only Phase 2.9.1 / Phase 2 closeout before Phase 2 can close as non-live readiness. Docs-only; no runtime code, UI, upload flow, report mapping, parser, scoring, fixtures, ProductPhotoReviewPanel routing, provider/storage/integration/case-queue/OCR/metadata work, receipt behavior change, commit, push, or deployment was performed.
- 2026-05-31: Phase 2.8.3 docs-only source-of-truth alignment after the review-only closeout retry passed for pushed commit `3dd8b74` (`fix: remove forbidden dev bridge wording`). Updated status docs to mark the Phase 2.8.2 closeout blocker patch reviewed and pushed, mark Phase 2.8.2 closed, record that `/dev/unsupported-evidence-review` remains production-blocked with a 404 under `next start`, record no deployment, and keep Phase 2.9 as planning-only next. No runtime code, UI, upload flow, live report mapping, parser, scoring, fixtures, ProductPhotoReviewPanel routing, provider/storage/integration/case-queue/OCR/metadata work, receipt behavior change, commit, push, or deployment was performed.
- 2026-05-31: Phase 2.8.2 closeout blocker patch. Replaced the dev-only unsupported-evidence bridge score-boundary notice with safe receipt-verification-outcome wording and tightened `scripts/check-report-semantics.mjs` so simple split/joined string literals are materialized before forbidden visible wording checks. The patch was later reviewed and pushed at `3dd8b74` during the Phase 2.8.3 closeout retry; Phase 2.8.2 is now closed and Phase 2.9 remains planning-only next. No live runtime wiring, `ClaimReviewWorkflow` insertion, ProductPhotoReviewPanel routing, upload/report/parser/scoring/fixture changes, receipt behavior changes, provider/storage/integration/case-queue/OCR/metadata work, or deployment was performed.
- 2026-05-31: Phase 2.8.2 dev-only unsupported-evidence review bridge implementation. Added `src/app/dev/unsupported-evidence-review/page.tsx`, a separate unlinked production-disabled route for synthetic unsupported-evidence review states, plus `src/app/dev/unsupported-evidence-review/render-cases.ts` with literal `UnsupportedEvidenceReviewDisplay`-shaped cases only. The route renders evidence type labels, unsupported/manual-review status, customer-safe wording, manual-review guidance, confidence/uncertainty treatment, blocked output reasons, allowed next actions, internal notes, external-verification-not-performed markers, and no-live isolation markers. It uses at most type-only imports from `unsupported-evidence-review-state`, does not value-import/call the mapper, does not import the workflow boundary helper, and does not import or call `analyzeEvidenceFile`, live report adapter mapping, upload, parser, scoring, fixtures, ProductPhotoReviewPanel, providers/storage/integrations/case queues, OCR/metadata, files, object URLs, or real evidence. Extended `scripts/check-report-semantics.mjs` with route/render-case production guard, import-boundary, privacy, wording, required-safe-concept, and no-link checks. Updated status docs only. Product-photo runtime remains non-live; unsupported-evidence live UI still does not exist; `ClaimReviewWorkflow` remains unwired; receipt behavior is unchanged; Phase 2.9 was not started; no deployment, commit, or push was performed.
- 2026-05-31: Phase 2.8.1 docs-only unsupported-evidence dev bridge implementation plan. Added `PHASE_2_8_1_UNSUPPORTED_EVIDENCE_DEV_BRIDGE_IMPLEMENTATION_PLAN.md` and updated status/source-of-truth docs to prepare the exact future Phase 2.8.2 implementation shape: a route-local, unlinked, production-disabled, synthetic-only developer bridge at `/dev/unsupported-evidence-review` using `src/app/dev/unsupported-evidence-review/page.tsx` plus `render-cases.ts`. The plan requires `process.env.NODE_ENV`/`notFound()` production blocking, literal `UnsupportedEvidenceReviewDisplay`-shaped cases with at most type-only mapper-contract imports, no workflow-boundary helper import, no mapper/wrapper/workflow execution, visible manual-review/customer-safe/uncertainty/blocked-output fields, and forbidden receipt-score/authenticity-score/proof/product-photo-report/fraud-accusation/automatic-decision fields. It lists eligible/protected files, semantic/privacy checks, browser/manual QA, implementation blockers, and Phase 2.9 gates. Docs-only; no runtime, route, UI, live caller wiring, upload, report mapping, parser, scoring, fixture, provider/storage/integration/case-queue, OCR/metadata, deployment, commit, or push work was performed.
- 2026-05-31: Phase 2.8.0 docs-only unsupported-evidence dev bridge acceleration plan. Added `PHASE_2_8_0_UNSUPPORTED_EVIDENCE_DEV_BRIDGE_PLAN.md` and updated status/source-of-truth docs to choose the safest future dev-only/user-visible bridge: a separate unlinked, production-disabled story/probe route with literal synthetic unsupported-evidence review states. The plan rejects `/test-evidence` extension, live `ClaimReviewWorkflow` insertion, ProductPhotoReviewPanel routing, live UI, and a pure probe-only follow-up as the first bridge; protects `analyzeEvidenceFile`, `LocalAnalysisResult`, live receipt report mapping, upload flow, parser, scoring, fixtures, providers/storage/integrations/case queues, OCR/metadata, deployment config, product-photo runtime, and receipt behavior; and blocks Phase 2.9 until Phase 2.8 defines or proves the dev-only/user-visible bridge. Docs-only; no runtime, UI, route, upload, report mapping, parser, scoring, fixture, provider/storage/integration/case-queue, OCR/metadata, deployment, commit, or push work was performed.
- 2026-05-31: Phase 2.7.8 docs-only source-of-truth alignment after pushed commit `32d013b` (`feat: add workflow boundary probe for unsupported evidence`). Updated status docs to mark Phase 2.7.7 pushed/closed, remove stale Phase 2.7.5 next-work wording, and recommend Phase 2.8.0 as a high-gear but safe docs-only acceleration plan toward dev-only harness/UI readiness. Phase 2.9 remains blocked until Phase 2.8 defines or proves the dev-only/user-visible bridge. Docs-only; no runtime, UI, upload, live report mapping, analyzer, parser, scoring, fixtures, ProductPhotoReviewPanel routing, providers/storage/integrations/case queues, OCR/metadata, deployment, commit, or push work was performed.
- 2026-05-31: Phase 2.7.7 probe-only workflow/caller boundary helper implementation. Added `src/lib/analysis/workflow-pre-analysis-gate-boundary.ts`, a pure default-off helper that models the future workflow boundary as either receipt-analysis delegation or an unsupported-evidence stopped state. The helper is not imported by `ClaimReviewWorkflow` or live UI/report/analyzer paths, does not call `analyzeEvidenceFile`, does not call `mapLocalAnalysisToReport`, does not produce or migrate `LocalAnalysisResult`, does not route `ProductPhotoReviewPanel`, and adds no rendered UI. Added `src/lib/analysis/workflow-pre-analysis-gate-boundary.probe.ts` to prove default-off receipt delegation, enabled unsupported stop-state derivation through the existing unsupported-evidence mapper, no report mapping for unsupported outcomes, no receipt-score/proof/product-photo-report wording, no private/raw evidence surfaces, and no live caller imports. Registered the probe in `scripts/run-product-photo-probes.cjs` (14 modules) and extended `scripts/check-report-semantics.mjs` with workflow-boundary semantic/import/privacy guards. Product-photo runtime remains non-live; pre-analysis wrapper remains unwired; receipt behavior remains unchanged; no providers/storage/integrations/case queues/OCR/metadata/deployment work was added. Pushed and closed at `32d013b` (`feat: add workflow boundary probe for unsupported evidence`); no deployment was performed.
- 2026-05-31: Phase 2.7.5 docs-only workflow/caller boundary opt-in design. Added `PHASE_2_7_5_WORKFLOW_CALLER_BOUNDARY_OPT_IN_DESIGN.md` and updated status/source-of-truth docs to design the future default-off caller opt-in path without implementation. The design names the future owner as the `ClaimReviewWorkflow` run-analysis boundary, or a small workflow-local helper used only from that boundary; keeps `analyzeEvidenceFile` receipt-only; keeps `LocalAnalysisResult` receipt-shaped; keeps unsupported-evidence output separate from receipt reports and ProductPhotoReviewPanel; limits later mapper use to derived-state mapping outside live receipt analysis; and records future eligible/protected files, default-off guard protections, browser/manual QA gates, required checks, and stop conditions. Docs-only; no runtime, UI, upload, live report mapping, ProductPhotoReviewPanel routing, analyzer entrypoint, receipt behavior, parser/scoring/fixtures, provider/storage/integration/case-queue, OCR/metadata, deployment, commit, or push work was performed.
- 2026-05-31: Phase 2.7.4 docs-only live-opt-in readiness checkpoint. Added `PHASE_2_7_4_LIVE_OPT_IN_READINESS_CHECKPOINT.md` and updated status/source-of-truth docs to decide the next safest readiness step after the non-live unsupported-evidence mapper/probe. The checkpoint chooses workflow/caller boundary design next, keeps any future opt-in at the caller boundary rather than inside `analyzeEvidenceFile`, keeps `LocalAnalysisResult` receipt-shaped, keeps unsupported-evidence output separate from receipt reports and ProductPhotoReviewPanel, rejects a dev-only harness/report-adapter shape probe/feature-flag plan as premature next moves, defines conditions before any live UI/runtime opt-in, records browser/manual QA requirements, forbidden wording, and protected surfaces. Docs-only; no runtime, UI, upload, live report mapping, ProductPhotoReviewPanel routing, analyzer entrypoint, receipt behavior, parser/scoring/fixtures, provider/storage/integration/case-queue, OCR/metadata, deployment, commit, or push work was performed.
- 2026-05-31: Phase 2.7.3 docs-only closeout alignment. Updated status docs to mark Phase 2.7.3 closed and pushed at `0d90260` (`docs: close phase 2.7.3 mapper probe`) and replaced the stale `NEXT_STEPS.md` recommended prompt that still pointed at the completed commit step. The new recommendation is pause or a docs-only Phase 2.7.4 planning checkpoint for the next live-opt-in readiness question, only with explicit Robert approval. Docs-only; no runtime, UI, upload, report mapping, parser, scoring, fixtures, ProductPhotoReviewPanel, provider/storage/integration/case-queue, OCR/metadata, package script, deployment, commit, or push work was performed.
- 2026-05-31: Phase 2.7.3 non-live unsupported-evidence display/mapping probe implementation. Added `src/lib/analysis/unsupported-evidence-review-state.ts`, a pure import-free mapper from sanitized synthetic unsupported-evidence case kinds into the `unsupportedEvidenceReview` display object. Added `src/lib/analysis/unsupported-evidence-review-state.probe.ts` covering product-photo, order screenshot, ambiguous PDF, unknown evidence, mixed evidence, unsupported image, receipt-like-not-parseable, legacy terminology, and hostile private sentinel omission; the probe asserts manual-review-only/customer-safe wording, required safe concepts, forbidden visible wording, no receipt/product-photo report display markers, no OCR/metadata/analyzer/live report adapter/UI/upload/parser/scoring/fixture/provider/storage/integration/case-queue paths, no ProductPhotoReviewPanel routing, unchanged `analyzeEvidenceFile` signature, and unchanged receipt-shaped `LocalAnalysisResult`. Registered the probe in `scripts/run-product-photo-probes.cjs` (13 imported modules) and added semantic/import/privacy coverage in `scripts/check-report-semantics.mjs`. Updated status docs only. No rendered UI, live runtime wiring, upload flow, live report adapter mapping, ProductPhotoReviewPanel routing, receipt behavior, parser/scoring/fixtures, provider/storage/integration/case-queue, OCR/metadata, deployment, commit, or push work was added.
- 2026-05-31: Phase 2.7.2 docs-only unsupported-evidence display/mapping probe planning. Added `PHASE_2_7_2_UNSUPPORTED_EVIDENCE_DISPLAY_MAPPING_PROBE_PLAN.md` to define a future pure non-live mapper/probe for derived unsupported-evidence review state. The plan covers no-score/no-proof-of-purchase/no-product-photo-report semantics, manual-review guidance, customer-safe copy, confidence/uncertainty display, receipt-preservation requirements, recommended future probe location, synthetic cases, checks, protected surfaces, and stop conditions. Updated status docs only. No runtime, UI, upload, live report mapping, ProductPhotoReviewPanel routing, provider/storage/integration/case-queue, OCR/metadata, fixture, parser, scoring, analyzer entrypoint, deployment, commit, or push work was performed.
- 2026-05-30: Phase 2.7.1 docs-only unsupported-evidence reviewer state contract. Added `PHASE_2_7_1_UNSUPPORTED_EVIDENCE_REVIEWER_STATE_CONTRACT.md` to define the future unsupported stop-state name (`unsupportedEvidenceReview`), allowed outcomes, neutral labels, reviewer fields, support-rep/customer-safe wording, confidence/uncertainty rules, forbidden display surfaces, receipt-preservation contract, future implementation gates, QA/browser expectations, protected surfaces, and stop conditions. Updated status docs only. No runtime, UI, upload, live report mapping, ProductPhotoReviewPanel routing, provider/storage/integration/case-queue, OCR/metadata, fixture, parser, scoring, analyzer entrypoint, deployment, commit, or push work was performed.
- 2026-05-30: Phase 2.7.0 docs-only live-opt-in readiness planning. Added `PHASE_2_7_LIVE_OPT_IN_READINESS_PLAN.md` to define the narrowest safe path before any future live caller can opt into the default-off `analyzeEvidenceFileWithPreAnalysisGate` wrapper. The plan recommends future opt-in at the workflow/caller boundary rather than inside `analyzeEvidenceFile`, requires an unsupported-evidence reviewer state contract before runtime/UI implementation, keeps `LocalAnalysisResult` receipt-shaped, preserves receipt behavior, and blocks product-photo runtime, upload/UI/report mapping, ProductPhotoReviewPanel routing, providers/storage/integrations/case queues, OCR/metadata, fixtures, parser, scoring, analyzer entrypoints, real evidence, deployment, commit, and push. Updated status docs only.
- 2026-05-30: Phase 2.6 source-of-truth alignment records Phase 2.6 closed after pushed commit `6f3170a` (`fix: tighten unsupported evidence wording guards`). Closeout review confirmed `main` clean and synced with `origin/main`, product-photo runtime non-live, the pre-analysis gate runtime wrapper default-off and unwired, `analyzeEvidenceFile` unchanged, `LocalAnalysisResult` unchanged, receipt behavior unchanged, UI/upload/report/scoring/parser/fixtures untouched, no ProductPhotoReviewPanel routing added by `6f3170a`, no providers/storage/integrations/case queues, no OCR/metadata processing, and no deployment. Docs/status only.
- 2026-05-30: Phase 2.6.4 blocker patch tightened unsupported evidence wrapper wording and guard coverage only. Replaced unsupported/quarantine customer and limitation text so it stays manual-review-only without proof, receipt-score, product-photo-report, verification, final-outcome, approval/rejection, or confirmed-wrongdoing wording; renamed the unsupported-result report marker to `futureReviewOutputProduced`; added runtime probe coverage for forbidden unsupported-output prose and semantic coverage for the unsupported-result builder plus probe assertions. No live runtime wiring, UI/upload/report mapping, analyzer, `LocalAnalysisResult`, receipt parser/scoring/fixtures, provider/storage/integration/case-queue, OCR/metadata, route, or deployment work was added. Verified with lint, build, report-semantics, product-photo-probes, diff-check, targeted unsupported-output wording scans, and protected-file diff scans. Committed and pushed as `6f3170a`.

## Log Entries

- 2026-05-30: Phase 2.6.2 default-off pre-analysis gate wrapper/probe implementation. Added `src/lib/analysis/pre-analysis-evidence-gate-runtime.ts`, a separate unwired wrapper entrypoint (`analyzeEvidenceFileWithPreAnalysisGate`) with `ENABLE_PRE_ANALYSIS_EVIDENCE_GATE_RUNTIME_GUARD: boolean = false`, a privacy-safe File-to-hints adapter (`buildPreAnalysisEvidenceGateHintsFromFile`) that reads only `file.name`, `file.type`, and optional declared/source category hints, and a local additive `UnsupportedEvidenceResult` / `PreAnalysisGateRuntimeResult` union. Default-off behavior delegates to the existing receipt analyzer path without running the gate; enabled allow outcomes delegate to the existing receipt analyzer; enabled non-allow outcomes (`unsupported-evidence`, `legacy-damage-photo-quarantine`, `product-photo-like-unsupported`, `unknown-inconclusive`) return manual-review-only unsupported results before the analyzer call. Added `src/lib/analysis/pre-analysis-evidence-gate-runtime.probe.ts` proving default-off delegation, allow-path delegation, non-allow analyzer blocking, mixed receipt/damage cue quarantine, private filename sentinel omission, no score/risk/report fields on unsupported results, no raw bytes/object URL/EXIF/raw metadata/provider/storage/integration/case-handle use, live-path import isolation, unchanged `analyzeEvidenceFile` signature, and unchanged receipt-shaped `LocalAnalysisResult`. Registered the probe in `check:product-photo-probes` (now 12 modules) and added semantic/import/privacy guard coverage for the wrapper/probe. Updated status docs. The wrapper remains unwired from `ClaimReviewWorkflow`, upload, live report adapter mapping, routes, product-photo analyzer/routing adapter, `ProductPhotoReviewPanel`, providers, storage, integrations, and case queues. No `analyzeEvidenceFile`, `LocalAnalysisResult`, receipt parser/scoring/fixtures, UI/upload/report behavior, real photos, real metadata fixtures, deployment, or push work was added.
- 2026-05-30: Phase 2.6.1 docs-only guard-only gate-wiring design spike. Added `PHASE_2_6_GATE_WIRING_DESIGN_SPIKE.md` to resolve the future first runtime-facing guard branch before implementation. The plan recommends a default-off thin pre-analyzer wrapper (`analyzeEvidenceFileWithPreAnalysisGate`) rather than an in-place `analyzeEvidenceFile` branch or UI-level branch; defines a privacy-safe File-to-hints adapter using filename/MIME/declared category only; defines an additive `UnsupportedEvidenceResult`/`GuardedEvidenceAnalysisResult` shape that avoids `LocalAnalysisResult` migration; lists receipt-preservation probes, gate-wiring probes, allowed Phase 2.6.2 files, protected files, stop conditions, and the recommended Phase 2.6.2 default-off wrapper/probe implementation. Updated `PHASE_2_6_RUNTIME_SCOPE_PLAN.md`, `PRODUCT_PHOTO_RUNTIME_BLOCKERS_PLAN.md`, `PRODUCT_PHOTO_UNSUPPORTED_BOUNDARY_PLAN.md`, `ROADMAP.md`, and `NEXT_STEPS.md` with narrow status pointers. Docs-only; no code, routes, React components, analyzer/runtime files, upload files, live report adapter mapping, `LocalAnalysisResult`, receipt parser/scoring/fixtures, product-photo analyzer/report/UI, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, or push work was added. Product-photo runtime remains non-live; pre-analysis evidence gate and adapter readiness remain decision-only and unwired.
- 2026-05-28: Ran Phase 2.4.9 as a docs-only pre-OCR/pre-metadata unsupported-boundary planning slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes, with UI/Product future-readiness covered locally. Added `PRODUCT_PHOTO_UNSUPPORTED_BOUNDARY_PLAN.md` and updated status docs to define the future no-live `pre-analysis-evidence-gate` contract/probe plan. Reconciled that the boundary should run conceptually before OCR, metadata inspection, receipt parsing, receipt scoring, and live report mapping; stop product-photo-like synthetic filename/type hints with decision-only unsupported/quarantine outputs; preserve receipt image, PDF receipt, order screenshot, `analyzeEvidenceFile`, `LocalAnalysisResult`, report adapter, receipt parser, receipt scoring, and fixtures; and keep product-photo non-live/manual-review-only. Recommended Phase 2.4.10 as no-live `pre-analysis-evidence-gate` contract/probe implementation before dev-only adapter harness work. No runtime code, routes, React components, analyzer runtime code, upload files, live report adapter mapping, `LocalAnalysisResult`, receipt parser/scoring/fixtures, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, push, or live product-photo work was added.
- 2026-05-28: Ran Phase 2.4.8 as a review-only post-classifier quarantine readiness checkpoint after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Confirmed `main` is clean and synced with `origin/main` at `8335e09` (`fix: quarantine legacy damage photo classifier`). Reconciled that the legacy `damage-photo` classifier quarantine is complete enough for its intended no-live scope: product/damage/photo/crack image filename cues collapse to the existing receipt/default classifier output instead of `damage-photo`; product-photo runtime remains non-live; `runtimeLive` remains false; `manualReviewOnly` remains true; `analyzeEvidenceFile` remains receipt-shaped; `LocalAnalysisResult` remains unchanged; and receipt behavior remains unchanged. Also reconciled the residual risk that product-photo-like filenames still enter the existing receipt/default OCR/metadata analyzer path after classifier collapse. Updated status docs only to recommend Phase 2.4.9 as docs-only pre-OCR/pre-metadata unsupported-boundary planning before dev-only harness, UI, upload, live report mapping, runtime, provider, storage, integration, or case workflow work. No runtime code, routes, React components, analyzer runtime code, upload files, live report adapter mapping, `LocalAnalysisResult`, receipt parser/scoring/fixtures, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, push, or live product-photo work was added.
- 2026-05-28: Ran Phase 2.4.6 as a narrow no-live legacy `damage-photo` classifier quarantine hardening slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Moved the live filename/evidence-type classifier boundary into `src/lib/analysis/analyzer-classifier.ts`, kept `src/lib/analysis/analyzer.ts` as the receipt-shaped `analyzeEvidenceFile` runtime entrypoint, and changed legacy damage/product/photo/crack image filename cues so they collapse to the existing receipt/default path instead of returning `damage-photo`. Added `src/lib/analysis/analyzer-classifier.probe.ts` for receipt/PDF/screenshot/null/default and legacy filename-cue classifier coverage, registered it plus the existing analyzer-routing guard probe in `check:product-photo-probes`, and tightened `scripts/check-report-semantics.mjs` so classifier quarantine markers, analyzer-routing non-live guard coverage, analyzer-routing forbidden imports, and active probe registrations are checked. Fixed the formerly inactive analyzer-routing probe assertions so the guard probe can run actively. Updated status docs to mark Phase 2.4.6 complete and recommend Phase 2.4.7 as review-only classifier quarantine closeout, while noting this is classifier-label hardening only and not a pre-OCR/pre-metadata unsupported boundary. Product-photo runtime remains non-live; `damage-photo` remains legacy/non-canonical; `LocalAnalysisResult`, upload/UI/live report adapter, receipt scoring/parser/fixtures, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, and push work were not added.
- 2026-05-28: Ran Phase 2.4.5 as a docs-only legacy `damage-photo` quarantine/migration planning slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, Deployment & Release, and UI/Product Workflow Lead review passes. Added `LEGACY_DAMAGE_PHOTO_QUARANTINE_PLAN.md` and updated status docs to define the inventory categories, canonical naming rule, first-boundary rule, receipt-preservation criteria, semantic/probe gates, browser/privacy gates, stop conditions, and recommended Phase 2.4.6 milestone. Reconciled that the safest first future hardening boundary is filename/evidence-type classification before the live receipt-shaped analyzer path proceeds; `product-photo` remains canonical, and `damage-photo` remains legacy receipt-era/mock compatibility terminology only. Recommended Phase 2.4.6 as no-live legacy `damage-photo` classifier quarantine hardening with `analyzeEvidenceFile`, `LocalAnalysisResult`, upload/UI/live report mapping, receipt scoring/parser/fixtures, providers, storage, integrations, case queues, real photos, and real metadata fixtures protected unless explicitly approved. No runtime code, routes, React components, analyzer/runtime code, upload files, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, or push work was added.
- 2026-05-27: Ran Phase 2.4.4 as a docs-only adapter readiness closeout and next-runtime-blockers planning slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, Deployment & Release, and UI/Product Workflow Lead review passes. Added `PRODUCT_PHOTO_RUNTIME_BLOCKERS_PLAN.md` and updated status docs to close Phase 2.4 adapter readiness planning as complete enough for the non-live checkpoint. Reconciled that passing adapter readiness probes means contract/shape acceptance only, not runtime readiness. Identified remaining runtime blockers: legacy live receipt-era `damage-photo` filename classification can still reach the receipt-shaped analyzer path, `readinessAccepted`/`inputKind` consumer gating is not live, dev-only harness remains unimplemented, no upload classification, no live report mapping, no `LocalAnalysisResult` migration, no real photos/metadata fixtures, no provider/storage/integration/case workflow, no formal browser QA script for rendered harnesses, unregistered probes must not be assumed active, and Phase 2.4 docs are not currently covered by `check:report-semantics`. Recommended Phase 2.4.5 as docs-only legacy `damage-photo` quarantine/migration planning before any no-live code hardening. No runtime code, routes, React components, analyzer/runtime code, upload files, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, or push work was added.
- 2026-05-27: Ran Phase 2.4.3 as a docs-only dev-only adapter review harness planning slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, Deployment & Release, and UI/Product Workflow Lead review passes. Added `PRODUCT_PHOTO_DEV_HARNESS_PLAN.md` and updated status docs to define a future synthetic-only, display-only adapter readiness review harness scope. The plan requires literal synthetic readiness cases only, visible `readinessAccepted`/`inputKind`/`runtimeLive: false`/`manualReviewOnly: true` gates, unsupported and legacy `damage-photo` quarantine states, no route-level adapter execution, no uploads/files/photos/raw metadata/identifiers, no live imports, semantic/privacy/import guards, browser QA criteria, and stop conditions. Updated `NEXT_STEPS.md` so it no longer points at the completed Phase 2.4.1 closeout prompt and recommends Phase 2.4.4 as docs-only adapter readiness closeout and next-runtime-blockers planning. No harness code, routes, React components, analyzer/runtime code, upload files, live report adapter mapping, `LocalAnalysisResult`, receipt parser/scoring/fixtures, providers, storage, integrations, case queues, real photos, real metadata fixtures, deployment, or push work was added.
- 2026-05-27: Patched the Phase 2.4.1 guarded non-live product-photo adapter contract nested report-view-model `damage-photo` quarantine gap after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Kept the fix patch-only inside the isolated adapter readiness boundary, readiness probe, and semantic marker guard. `prepareProductPhotoAdapterReadinessForDevOnlyBoundary` now routes nested report-view-model `viewModel.evidenceType === "damage-photo"` through the same legacy damage-photo quarantine result as top-level and nested analysis-result damage-photo inputs. Strengthened the adapter readiness probe with hostile nested report-view-model damage-photo coverage for explicit top-level product-photo and omitted top-level safe-shape wrappers, non-live/manual-review-only assertions, privacy sentinel omission checks, and report-semantics marker coverage. No product-photo runtime routing, upload wiring, `analyzeEvidenceFile`, analyzer routing, UI insertion, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-27: Patched the Phase 2.4.1 guarded non-live product-photo adapter contract top-level evidence-type mismatch blocker after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Kept the fix patch-only inside the isolated adapter readiness boundary, readiness probe, and semantic marker guard. `prepareProductPhotoAdapterReadinessForDevOnlyBoundary` now accepts analysis-result and report-view-model adapter inputs only when top-level `evidenceType` is absent for the safe internal/probe shape or explicitly canonical `product-photo`; top-level receipt, unknown, unsupported, or other non-product-photo values collapse to unsupported output, while top-level or nested `damage-photo` remains legacy quarantine-only. Strengthened the adapter readiness probe with top-level mismatch coverage for canonical nested analysis-result and report-view-model inputs, safe omitted top-level cases, non-live/manual-review-only assertions, and report-semantics marker coverage. No product-photo runtime routing, upload wiring, `analyzeEvidenceFile`, analyzer routing, UI insertion, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-27: Patched the Phase 2.4.1 guarded non-live product-photo adapter contract blocker after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Kept the fix patch-only inside the isolated adapter readiness boundary and probe. `prepareProductPhotoAdapterReadinessForDevOnlyBoundary` now accepts nested analysis-result input only when `result.evidenceType` is canonical `product-photo`; nested `damage-photo` is quarantined as legacy compatibility-only, while nested receipt, unknown, unsupported, or missing evidence types collapse to unsupported non-live manual-review-only output. Strengthened the adapter readiness probe with hostile nested damage-photo coverage, unsupported nested evidence-type collapse coverage, runtime-live/manual-review-only assertions, and privacy sentinel omission checks. No product-photo runtime routing, upload wiring, `analyzeEvidenceFile`, analyzer routing, UI insertion, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-27: Ran Phase 2.4.1 as a guarded non-live product-photo adapter contract/probe implementation slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, Deployment & Release, and UI/Product Workflow Lead review passes. Kept product-photo runtime non-live and unwired. Added `prepareProductPhotoAdapterReadinessForDevOnlyBoundary` inside the isolated product-photo adapter module to accept sanitized `ProductPhotoEvidenceAnalysisResult` or `ProductPhotoReportViewModel` data, derive/canonicalize score, confidence, review priority, local signal level, source kind, summary, support action, customer wording, limitations, and signals, force manual-review-only/non-live isolation flags, and collapse unsupported inputs. Added an active adapter-readiness probe for canonical result/view-model inputs, hostile override collapse, low/medium/high score scope, legacy `damage-photo` quarantine, unsupported receipt-like input collapse, privacy sentinel omission, exact metadata omission, and protected import boundaries. Updated the older dev routing adapter so legacy `damage-photo` is quarantine-only and no longer builds product-photo details. Registered the probe in `check:product-photo-probes` and tightened semantic guards for adapter readiness coverage and unsafe product-photo wording. No upload routing, `analyzeEvidenceFile`, analyzer routing, UI insertion, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-27: Ran Phase 2.4.0 as a docs-only guarded non-live product-photo adapter readiness planning slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, Deployment & Release, and UI/Product Workflow Lead review passes. Added `PRODUCT_PHOTO_ADAPTER_READINESS_PLAN.md` and updated status docs to mark Phase 2.3 analyzer hardening closed and Phase 2.4 planning open. Defined the future non-live adapter contract boundaries, score/safety derivation rules, customer-safe manual-review language requirements, raw metadata/privacy exclusions, legacy `damage-photo` quarantine plan, semantic/probe gates, blocked live-runtime gates, likely allowed/protected Phase 2.4.1 files, and pass/fail criteria. No adapter code, runtime routing, upload wiring, UI insertion, live report adapter mapping, `LocalAnalysisResult` migration, receipt behavior change, provider/storage/integration/case queue work, real photos, or real metadata fixtures were added.
- 2026-05-27: Ran Phase 2.3.5 as a no-live-wiring product-photo analyzer readiness hardening slice after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews. Kept product-photo non-live and unwired. Derived product-photo score, local signal level, review priority, and confidence from sanitized local details instead of trusting caller structured overrides; added runtime allowlist canonicalization for product-photo subject/context/damage/requested-view/file-summary/metadata/signal inputs; added direct-boundary hostile structured override probe coverage; added a narrow executable `check:product-photo-probes` script that imports non-live product-photo probe modules; tightened semantic guards for structured override regressions, recognition/routing probe coverage, probe-runner coverage, and canonical product-photo separation from legacy `damage-photo`. Updated status docs. No upload routing, `analyzeEvidenceFile`, analyzer routing edits, UI insertion, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-27: Ran Phase 2.3.4 Product Photo Analyzer Boundary Readiness Review as the Main Supervisor with Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews. Kept the pass review/status-only: no product-photo routing, upload wiring, UI insertion, live report adapter mapping, provider/storage/integration/case queue work, real-photo support, `analyzeEvidenceFile` edits, `LocalAnalysisResult` changes, or receipt behavior changes. Reconciled that the current non-live canonical `product-photo` analyzer boundary is safe enough for another guarded, non-live hardening slice after Phase 2.3.3, but live integration remains blocked until score/confidence/review-priority/local-signal overrides are derived or clamped, runtime enum/string canonicalizers are strengthened, executable probe coverage is added, and the legacy live `damage-photo` receipt-era path is documented/quarantined from canonical `product-photo` work. Updated only status docs.
- 2026-05-27: Ran Phase 2.3.3 as a privacy-only raw-result override sanitizer/probe pass after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews. Kept product-photo non-live and unwired. Tightened `prepareProductPhotoEvidenceAnalysisResultForDevOnlyBoundary` so direct caller overrides for product-photo result narrative summary, recommended support action, customer-safe wording, review label, and non-local source kind are canonicalized by the analyzer boundary instead of passed through. Strengthened `src/lib/analysis/product-photo-analyzer.probe.ts` with a direct-boundary hostile narrative override case and tightened `scripts/check-report-semantics.mjs` with static guards against raw override propagation. No upload routing, `analyzeEvidenceFile`, analyzer routing, UI, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-26: Ran a Phase 2.3.2 Main Supervisor checkpoint review after the pushed Phase 2.3.1 product-photo heuristic probe refinement. Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews found the unwired analyzer boundary clean, product-photo runtime non-live, safety/privacy probe coverage strong, and no coupling to upload, `analyzeEvidenceFile`, analyzer routing, UI, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, or case queues. Reconciled that one tiny privacy-only raw-result override sanitizer/probe pass is prudent before a guarded analyzer-boundary/readiness task, because the older dev/probe-only raw product-photo result boundary still accepts narrative override fields even though the local heuristic wrapper and display view-model are sanitized. Updated `ROADMAP.md` and `PHASE_2_PHOTO_EVIDENCE_PLAN.md` only to correct Phase 2.3 status from ready-to-open to open with the first analyzer slice and Phase 2.3.1 closed/pushed.
- 2026-05-26: Ran a Phase 2.3.1 Main Supervisor refinement pass for the unwired product-photo heuristic analyzer after Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Kept product-photo runtime non-live and unwired. Added a narrow very-low-quality local analyzer bucket so stacked file-size/dimension limits map to `Poor` quality inside the product-photo-only boundary, canonicalized product-photo outputs to generic labels, bucket-only metadata, allowlisted metadata/label notes, and allowlisted quality-limit summaries, and strengthened `src/lib/analysis/product-photo-analyzer.probe.ts` with synthetic cases for very low quality, missing product context with visible damage context, complete product context with incomplete damage context, limited metadata summaries, privacy sentinel omission, confidence/readiness separation, triage-only review priority, limitations separation, and expanded private/raw surface omission. Tightened `scripts/check-report-semantics.mjs` to require the new probe markers and updated `NEXT_STEPS.md` status. No upload routing, `analyzeEvidenceFile`, analyzer routing, UI, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-26: Opened Phase 2.3 with the first unwired local product-photo heuristic analyzer slice after required Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist review passes. Added `analyzeProductPhotoEvidenceWithLocalHeuristics` in `src/lib/analysis/product-photo-analyzer.ts` as a thin, local-only, manual-review-only analyzer boundary that accepts synthetic or bucketed product-photo review context, composes the existing product-photo details/result boundary, derives result copy instead of propagating unsafe caller-provided narrative fields, and remains non-live. Strengthened `src/lib/analysis/product-photo-analyzer.probe.ts` with active shape, heuristic-output, safety, privacy, and isolation checks covering complete context, missing wider view, limited quality, unavailable metadata, serial/model label context with raw values omitted, proof-of-purchase-match-needed, hostile caller-copy sanitization, separate score/confidence/review-priority/limitations/recommended-action semantics, bucket-only metadata, and receipt preservation. Tightened `scripts/check-report-semantics.mjs` to include analyzer/heuristics probes and direct product-photo analyzer import/privacy/status guards. Updated `NEXT_STEPS.md` status. No upload routing, `analyzeEvidenceFile`, analyzer routing, UI, live report adapter mapping, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, raw metadata, original filenames, raw label values, image bytes, or object URLs were added.
- 2026-05-26: Ran a Phase 2.2 Main Supervisor closeout and Phase 2.3 readiness review with Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, UI/Product Workflow Lead, and Deployment & Release specialist passes. Reconciled that Phase 2.2 Product Photo Boundary and Display Readiness can close: shared result, product-photo result boundary, guarded analyzer-routing boundary, product-photo report view-model, isolated `ProductPhotoReviewPanel`, production-disabled synthetic render host, semantic/privacy coverage, and desktop/mobile browser QA are complete enough for the non-live readiness slice. Documented Phase 2.3 entry criteria and boundaries in `NEXT_STEPS.md`, updated `ROADMAP.md` phase status, and added a short closeout note to `PHASE_2_PHOTO_EVIDENCE_PLAN.md`. Product-photo runtime remains non-live. No runtime code, probes, `ClaimReviewWorkflow`, upload routing, `analyzeEvidenceFile`, analyzer behavior, report adapter, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-26: Ran a Phase 2.2 Main Supervisor implementation pass for the non-live synthetic `ProductPhotoReviewPanel` visual verification host, with Architecture & Maintainability, UI/Product Workflow Lead plus UI sub-team, Photo Evidence / Phase 2 Readiness, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release reviews before implementation. Added an unlinked, production-disabled developer route at `src/app/dev/product-photo-review-panel/page.tsx` plus colocated literal `ProductPhotoReportViewModel` cases in `src/app/dev/product-photo-review-panel/render-cases.ts`. The host renders only synthetic low-confidence, medium-priority, and stronger manual-review cases, displays no photo preview, uses no image URLs, object URLs, files, blobs, raw metadata, original filenames, raw labels, provider output, storage handles, integration handles, or case queue identifiers, and states it is a synthetic non-live visual QA surface. Tightened `scripts/check-report-semantics.mjs` so the host files are required and guarded for production-disabled behavior, synthetic view-model-only data, protected live-module import denial, privacy omissions, and non-linkage from the live workflow. Updated `NEXT_STEPS.md` status and next prompt. No `ClaimReviewWorkflow`, upload routing, `analyzeEvidenceFile`, analyzer routing, report adapter, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-26: Ran a Phase 2.2 Main Supervisor docs-only planning pass for a future non-live `ProductPhotoReviewPanel` render host, with Architecture & Maintainability, UI/Product Workflow Lead plus UI sub-team, Photo Evidence / Phase 2 Readiness, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release reviews before editing docs. Updated `NEXT_STEPS.md` and `PHASE_2_PHOTO_EVIDENCE_PLAN.md` only to recommend a future synthetic visual verification host while keeping the current task implementation-free. The plan prefers a non-route component/browser harness if tooling is added later; if URL browser checks are needed first, it recommends a separate unlinked, production-disabled developer route at `src/app/dev/product-photo-review-panel/page.tsx` with colocated literal `ProductPhotoReportViewModel` render cases, not `/test-evidence` and not `ClaimReviewWorkflow`. Documented required synthetic data contract, import boundaries, visual QA goals, privacy/screenshot constraints, semantic coverage, allowed/protected future files, stop conditions, and the next implementation prompt. No runtime code, route, host, component, probe, `ClaimReviewWorkflow`, upload routing, `analyzeEvidenceFile`, analyzer routing, report adapter, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-26: Ran a Phase 2.2 Main Supervisor implementation pass for the first isolated `ProductPhotoReviewPanel` component slice, with Architecture & Maintainability, UI/Product Workflow Lead plus UI sub-team, Photo Evidence / Phase 2 Readiness, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release reviews before implementation. Added `src/components/ProductPhotoReviewPanel.tsx` as a standalone, unwired product-photo evidence-review panel that accepts exactly `viewModel: ProductPhotoReportViewModel`, renders derived safe summaries only, keeps review status, priority, confidence, evidence quality, score meaning, product/photo context, recommended action, limitations, review signals, and privacy posture separate, and owns no image preview, file/blob/object URL, export, callback, or live workflow state. Added `src/components/ProductPhotoReviewPanel.probe.tsx` and tightened `scripts/check-report-semantics.mjs` so the new component and probe are included in semantic/import/privacy coverage, including ProductPhotoReportViewModel-only props, protected live-module import denial, raw/private field denial, manual-review-only copy, separate score/priority/confidence/limitations, receipt adapter preservation, and `ClaimReviewWorkflow` non-wiring. Updated `NEXT_STEPS.md` status and next prompt. No `ClaimReviewWorkflow`, upload routing, `analyzeEvidenceFile`, analyzer routing, report adapter, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-26: Ran a Phase 2.2 Main Supervisor docs-only planning pass for the first standalone product-photo display component slice, with Architecture & Maintainability, UI/Product Workflow Lead plus UI sub-team, Photo Evidence / Phase 2 Readiness, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release reviews before editing docs. Updated `NEXT_STEPS.md` and `PHASE_2_PHOTO_EVIDENCE_PLAN.md` only to define the future component as an isolated `ProductPhotoReviewPanel`-style evidence-review panel with exactly `viewModel: ProductPhotoReportViewModel`, no image preview ownership, no object URL/file/blob/raw metadata/raw result props, no live workflow insertion, no receipt report types, required sections for review status, priority, confidence, evidence quality, score meaning, product context, action, limitations, signals, privacy posture, semantic/privacy coverage requirements, browser/render check expectations when a renderable isolated host exists, allowed and protected future files, stop conditions, and the next implementation prompt. No component files, runtime code, probes, `ClaimReviewWorkflow`, upload routing, `analyzeEvidenceFile`, analyzer routing, report adapter, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-26: Ran a Phase 2.2 Main Supervisor implementation pass for the first isolated product-photo UI display contract probe slice, with Architecture & Maintainability, UI/Product Workflow Lead plus UI sub-team, Photo Evidence / Phase 2 Readiness, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release reviews before implementation. Strengthened `src/lib/analysis/product-photo-report-view-model.probe.ts` only for display-contract proof: missing-context and complete-context display cases, low/medium/high score semantics, missing metadata summary handling, raw label value omission, overconfident review-label clamping, recursive private-key audits, sentinel private-value omission, allowlisted view-model shape, and receipt/report-adapter preservation. Tightened `scripts/check-report-semantics.mjs` with targeted product-photo mapper and display-contract probe checks plus extra unsafe product-photo display wording patterns. Updated `NEXT_STEPS.md` status and next prompt. No UI component, `ClaimReviewWorkflow`, upload routing, `analyzeEvidenceFile`, analyzer routing behavior, live report adapter, `LocalAnalysisResult`, receipt behavior, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-25: Ran a Phase 2.2 Main Supervisor docs-only planning pass for the future product-photo report/UI display gate, with Architecture & Maintainability, UI/Product Workflow, Photo Evidence / Phase 2 Readiness, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release specialist reviews before editing docs. Updated `NEXT_STEPS.md` and `PHASE_2_PHOTO_EVIDENCE_PLAN.md` only to define what future product-photo UI display means and does not mean, required prerequisites, safe UI copy rules, allowed view-model fields, forbidden display/export fields, required probes/checks, allowed and protected future files, stop conditions, receipt UI preservation, and why upload routing, analyzer routing, provider behavior, and case workflow remain separate. No runtime code, probes, analyzer behavior, `LocalAnalysisResult`, `analyzeEvidenceFile`, product-photo runtime behavior, UI, upload, live report-adapter mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-25: Ran a Phase 2.2 Main Supervisor implementation pass for the isolated product-photo-safe report mapping/probe slice, with Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, Scoring & Safety, QA Harness, Privacy & Evidence Safety, and Deployment & Release specialist reviews before implementation. Added a separate dev/probe-only product-photo report view-model boundary in `src/lib/analysis/product-photo-report-view-model.ts` and a dedicated probe in `src/lib/analysis/product-photo-report-view-model.probe.ts`. The mapper consumes `ProductPhotoEvidenceAnalysisResult`, not `LocalAnalysisResult`, derives safe review summaries from allowlisted enums and buckets, keeps score, review priority, and confidence separate, forces external verification to not performed, clamps unsupported clear review labels to inconclusive, and omits raw photo bytes, image buffers, raw EXIF, raw metadata, original filenames, raw label values, provider output, storage handles, integration handles, and case queue handles. Expanded `scripts/check-report-semantics.mjs` to include product-photo result/mapping/probe files and scoped product-photo wording checks. `mapLocalAnalysisToReport`, `LocalAnalysisResult`, `analyzeEvidenceFile`, analyzer routing, UI, upload, scoring, parser, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, and private metadata were not touched.
- 2026-05-25: Ran a Phase 2.2 Main Supervisor docs-only planning pass for the future product-photo-safe report/UI mapping gate, with Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, Scoring & Safety, QA Harness, Privacy & Evidence Safety, and Deployment & Release specialist reviews before editing docs. Updated `NEXT_STEPS.md` and `PHASE_2_PHOTO_EVIDENCE_PLAN.md` only to define what future product-photo report/UI mapping means and does not mean, required prerequisites, safe language rules, result-shape constraints, privacy display/export limits, required probes/checks, allowed and protected future files, stop conditions, receipt-behavior preservation, and why live upload/analyze routing remains separate. No runtime code, probes, analyzer behavior, `LocalAnalysisResult`, `analyzeEvidenceFile`, product-photo runtime behavior, UI, upload, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, or private metadata changed.
- 2026-05-25: Ran a Phase 2.2 Main Supervisor implementation pass for the first guarded internal analyzer-routing slice, with Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews before implementation. Added `buildGuardedInternalAnalyzerRouteDecision` as a decision-only internal routing boundary in `src/lib/analysis/analyzer-routing.ts`; it reuses the existing recognition-only/file-aware guard, recognizes product-photo candidates as guarded unsupported live paths, preserves the existing receipt analyzer path decision, and explicitly records `productPhotoRuntimeLive: false`, `analyzerInvoked: false`, `adapterInvoked: false`, and `productPhotoResultBoundaryInvoked: false`. Tightened `src/lib/analysis/analyzer-routing.probe.ts` with guarded-internal-route cases, broader public-wrapper shared-result payload exclusion checks, synthetic metadata-bearing product-photo routing coverage, import/invocation boundary expectations, and probe import assertions. `analyzeEvidenceFile`, `LocalAnalysisResult`, product-photo result boundary behavior, UI, upload, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw EXIF, and private metadata were not touched.
- 2026-05-25: Ran a Phase 2.2 Main Supervisor docs-only planning pass for the future live analyzer-routing integration gate, with Architecture & Maintainability, Photo Evidence / Phase 2 Readiness, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews. Updated `NEXT_STEPS.md` only to define what the first future live-routing slice means and does not mean, required prerequisites before code changes, required probes, allowed files, protected files, stop conditions, required checks, and the recommended next implementation prompt. The plan keeps `analyzeEvidenceFile` receipt-only and unchanged for the first future routing slice, preserves receipt-shaped `LocalAnalysisResult`, keeps product-photo output out of UI/upload/report/scoring/parser/fixture/provider/storage/integration/case queue paths, keeps product-photo local-heuristics-only and manual-review-only, and keeps external verification not performed. No runtime code, probes, analyzer behavior, upload, UI, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw OCR, raw EXIF, or private metadata changed.
- 2026-05-25: Ran a Phase 2.2 Main Supervisor probe-only isolation pass for the non-live product-photo shared-result boundary, with Architecture & Maintainability, QA Harness, Scoring & Safety, Privacy & Evidence Safety, and Deployment & Release specialist reviews. Tightened only probe/status surfaces: `product-photo-result.probe.ts` now explicitly records no `analyzeEvidenceFile`, analyzer routing, UI, upload, report mapping, scoring, parser, fixtures, providers, storage, integrations, or case queue invocation; `shared-result.probe.ts` records the same isolation boundary and manual-review/external-verification expectations; `analyzer-routing.probe.ts` confirms the public wrapper does not expose shared-result payload fields. Probe privacy assertions now state sample data is synthetic and includes no file bytes, image buffers, raw EXIF objects, provider handles, storage handles, integration handles, or case queue handles. No runtime analyzer behavior, live routing, `LocalAnalysisResult`, `analyzeEvidenceFile`, analyzer routing implementation, UI, upload, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw OCR, raw EXIF, or private metadata changed.
- 2026-05-25: Added a Phase 2.2 non-live product-photo shared-result boundary in `src/lib/analysis/product-photo-analyzer.ts` plus a dedicated compile-only probe in `src/lib/analysis/product-photo-result.probe.ts`. The boundary prepares `ProductPhotoEvidenceAnalysisResult` / `EvidenceAnalysisResult` with `module: "productPhoto"` for dev/probe use only, keeps product-photo details nested under `moduleDetails.productPhoto`, keeps external verification as not performed / not externally verified, uses privacy-safe metadata summaries, and does not use `LocalAnalysisResult`. The probe confirms the boundary is dev-only/probe-only/non-live, product-photo does not require receipt-only OCR/parser/result fields, no live analyzer/UI/report paths are represented as invoked, and raw EXIF/original filename/product-label raw values stay omitted. `analyzeEvidenceFile`, analyzer routing, UI, upload, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw OCR, raw EXIF, and private metadata were not touched.
- 2026-05-25: Added a Phase 2.2 type-only shared `EvidenceAnalysisResult` envelope in `src/lib/analysis/types.ts` plus a compile-only shared-result probe in `src/lib/analysis/shared-result.probe.ts`. The shared result shape represents receipt and product-photo as separate module variants, keeps product-photo details nested under the product-photo module, confirms product-photo results do not require receipt-only OCR/parser/result fields, and keeps external verification as not performed / not externally verified. `LocalAnalysisResult` remains unchanged and receipt-path shaped. `analyzeEvidenceFile` was not touched, imported, or called by the new probe. No runtime analyzer behavior, live routing, UI, upload, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, real photos, raw OCR, raw EXIF, or private metadata were added.
- 2026-05-25: Added a docs-only Phase 2.2 staged live analyzer-routing integration plan to `NEXT_STEPS.md`. The plan keeps `routeAnalyzerEvidenceInput` decision-only and unwired, requires probe expansion before runtime wiring, allows type-only/shared-result refinement only if needed, keeps any product-photo adapter/result boundary non-live and separate from `LocalAnalysisResult`, requires a guarded internal route-to-adapter path before UI access, and requires separate approval before live analyzer/UI/upload integration. Documented live-use conditions for preserving receipt `LocalAnalysisResult` behavior, product-photo-safe report mapping, manual-review-only UI language, local-heuristics-only/not-externally-verified product-photo behavior, no real photos or integrations, and a guarded product-photo runtime flag. Documented required probes for receipt preservation, unchanged receipt analyzer output, PDF/screenshot classification-only behavior, non-live product-photo runtime flag, `damage-photo` alias handling, no product-photo leakage into `LocalAnalysisResult`, decision-only wrapper isolation, and safety-wording scan cleanliness. No runtime analyzer, analyzer-routing code, upload, UI, report mapping, scoring, parser, fixtures, providers, storage, integrations, or case queues changed.
- 2026-05-25: Updated Phase 2.2 status docs after pushed commit `2071374` added the decision-only public analyzer routing wrapper. `routeAnalyzerEvidenceInput` now exists as an exported wrapper returning `PublicAnalyzerRoutingDecision` only, remains unwired from live UI/upload/report/scoring/parser paths, does not call `analyzeEvidenceFile`, does not call the product-photo routing adapter, and keeps product-photo candidates guarded/non-live even when runtime routing is requested. `analyzeEvidenceFile` remains the live receipt analyzer entrypoint. No runtime analyzer behavior, upload behavior, UI, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, or real evidence changed. Next live routing work still requires a separate planning prompt before implementation.
- 2026-05-25: Updated Phase 2.2 status docs after pushed commit `7459a15` completed the dev-only analyzer-routing preservation/status probe. The probe confirms receipt-like inputs preserve the receipt path conceptually, product-photo candidates remain guarded as an unsupported live path when runtime routing is disabled, unknown inputs remain inconclusive, product-photo candidate output does not require `LocalAnalysisResult`, and `analyzer-routing` remains unimported by live UI, upload, report, scoring, and parser paths. No analyzer entrypoint, upload, UI, scoring, report mapping, parser behavior, fixtures, providers, storage, integrations, case queues, or real photos changed. Runtime product-photo routing remains blocked.
- 2026-05-25: Updated Phase 2.2 status docs after pushed commits `1e5c928` and `e8636e4` added the dev-only analyzer routing guard and optional file-aware routing boundary. The analyzer routing guard and optional file-aware boundary exist but remain dev-only and unwired, with runtime routing disabled by default, product-photo candidates guarded as an unsupported live path, and the receipt path preserved. They are not imported by live UI, upload, report, scoring, parser, or fixture paths. No analyzer routing, upload, UI, scoring, report mapping, parser behavior, fixtures, providers, storage, integrations, case queues, or real photos changed. Runtime routing remains blocked.
- 2026-05-25: Added a Phase 2.2 dev-only analyzer routing guard wrapper and synthetic developer probe. The wrapper returns its own guarded decision shape, uses the existing product-photo recognition boundary only, keeps product-photo runtime routing default off, marks product-photo candidates as an unsupported live path, preserves the conceptual receipt analyzer path for receipt-like evidence, and does not call the product-photo routing adapter. No `analyzeEvidenceFile`, `LocalAnalysisResult`, UI/upload behavior, report mapping, scoring, parser behavior, fixtures, providers, storage, integrations, case queues, or real photos changed. Ran `npm.cmd run lint`, `npm.cmd run build`, and `npm.cmd run check:report-semantics`.
- 2026-05-25: Recorded Phase 2.2 status after pushed commit `2cbe002` added the dev-only product-photo routing adapter. The routing adapter exists but remains dev-only and probe-only, composes the recognition boundary with the product-photo analyzer builder inside an isolated module, and returns its own adapter-specific result rather than `LocalAnalysisResult`. No analyzer routing, upload behavior, UI, scoring, report mapping, parser behavior, fixtures, providers, storage, integrations, case queues, or real photos changed. Runtime routing remains blocked until Robert explicitly opens that slice.
- 2026-05-25: Updated Phase 2.2 status docs after pushed commit `bf46949` added the dev-only product-photo recognition boundary. The boundary remains isolated and dev-only, with `recognizeProductPhotoEvidence` not called by `analyzeEvidenceFile`; product-photo helpers, compile probes, the exported-only analyzer builder, the analyzer probe, and the recognition boundary remain local-only and unwired. No analyzer routing, upload behavior, UI, scoring, report mapping, parser behavior, fixtures, providers, storage, integrations, case queues, or real photos changed. Runtime routing remains blocked until Robert explicitly opens that slice.
- 2026-05-25: Captured Robert's future Evidence Review UX direction as a docs-only product note in `NEXT_STEPS.md`. The note keeps Phase 2.2 runtime-boundary work implementation-free while recording that the future result screen should behave like an evidence triage workspace with one primary review state, signals as the centerpiece, future signal-to-region traceability, explainable score breakdowns, separate confidence and risk concepts, clear reviewer next actions, less repetitive idle-state guidance, local-only/privacy posture, and manual-review-only actions. No app UI, upload behavior, analyzer runtime, `analyzeEvidenceFile`, `LocalAnalysisResult`, scoring, report mapping, parser behavior, fixtures, provider calls, storage, integrations, case queues, or dependencies changed.
- 2026-05-25: Recorded current Phase 2.2 helper status after the first two helper commits were completed and pushed on `main`: `44d09f0` added unwired product-photo signal builders, and `50f8284` added unwired product-photo file summary, review completeness, and local review signal helpers. These helpers remain local-only, manual-review-support-only, provider-ready, and intentionally unwired from `analyzeEvidenceFile`, runtime analyzer routing, upload, UI, report mapping, scoring, parser, metadata extraction behavior, fixtures, storage, integrations, and real customer photos. Updated only `NEXT_STEPS.md` and `AGENT_LOG.md` for this documentation slice.
- 2026-05-25: Reviewed the Phase 2.1 product-photo local heuristic design as the Photo Evidence / Phase 2 Readiness Agent, with Scoring & Safety, Privacy & Evidence Safety, Product Strategy, QA Harness, and Deployment & Release as secondary concerns. Confirmed the Phase 2.1 signal design stays manual-review-only, local-only, privacy-safe, and unwired from analyzer runtime, upload, UI, scoring, parser, report mapping, fixtures, metadata extraction behavior, `LocalAnalysisResult`, and `analyzeEvidenceFile`. Made the smallest documentation correction in `AGENTS.md` and `ROUTING.md` to align agent guidance with the current Phase 2.1 review state and to keep Phase 2.2 runtime work blocked until Robert explicitly opens it. No runtime code, fixtures, provider calls, storage, integrations, or real photos changed. Ran `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`.
- 2026-05-25: Started Phase 2.1 as Product Photo Local Heuristic Design with the Photo Evidence / Phase 2 Readiness Agent as primary, and Product Strategy, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release as secondary concerns. Updated `ROADMAP.md`, `NEXT_STEPS.md`, and `PHASE_2_PHOTO_EVIDENCE_PLAN.md` as documentation-only planning work. Defined first-pass product-photo heuristic signal categories for photo quality limits review, product context incomplete, requested view missing or incomplete, damage visibility unclear, product label/context missing, metadata context unavailable or limited, image consistency uncertainty, findings inconclusive, and manual review recommended. Documented safe meanings, claims each signal must avoid, safe wording, local-later implementation candidates, provider/metadata-dependent waits, and a proposed Phase 2.2 implementation order. No product-photo runtime analyzer behavior, upload behavior, UI, scoring, report mapping, fixtures, receipt parser, metadata extraction behavior, runtime routing, storage, integrations, case queues, provider calls, real photos, `LocalAnalysisResult`, or `analyzeEvidenceFile` changed. Ran `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`.
- 2026-05-25: Phase 2.0 implementation officially started as safe scaffold/type-boundary work only after pushing the existing starter commit `cfe6f36`. Added an unwired product-photo default analysis scaffold in `src/lib/analysis/product-photo-analyzer.ts` and unwired local-heuristic label/default constants in `src/lib/analysis/product-photo-heuristics.ts`. The scaffold uses product-photo as the canonical Phase 2 type, keeps damage-photo only as existing compatibility context, stays local-heuristics-only/provider-ready, and uses manual-review-only wording such as manual review recommended, image consistency signal, photo quality limits review, product context incomplete, and findings inconclusive. No product-photo analyzer behavior is live yet, and no analyzer routing, scoring, receipt parser, metadata extraction, report mapping, UI, upload behavior, fixtures, storage, integrations, external AI/vision providers, or real customer photos were changed during the overnight Phase 2 scaffold. Slice 3 unit tests were skipped because no existing test script or unit-test pattern is configured.
- 2026-05-25: Strengthened Agent System Upgrade 1.0 operating rules as documentation/config guidance only. Added Main Agent challenge mode, required secondary-agent review triggers, stop-and-report conditions, task-type definitions of done, and handoff quality grading to `AGENTS.md`; mirrored the quality-control rules in `ROUTING.md`, `.codex/commands/claimguardagent.md`, `CLAIMGUARD_AGENT_COMMAND.md`, and `REPO_SOURCE_OF_TRUTH.md`. No app code, UI, upload mechanics, analyzer/parser/scoring/report/privacy logic, fixtures, dependencies, deploy behavior, Playwright, or Phase 2 implementation was changed.
- 2026-05-25: Refined Agent System Upgrade 1.0 docs to define the Main ClaimGuard Agent as the supervising orchestrator rather than a passive router. Updated `AGENTS.md`, `ROUTING.md`, `.codex/commands/claimguardagent.md`, and `CLAIMGUARD_AGENT_COMMAND.md` so the main agent classifies task and phase, selects and delegates to a specialized agent, defines scope, reviews output critically, challenges weak assumptions, unsafe wording, phase drift, generic UI choices, evidence-safety gaps, and incomplete checks, requires another pass when needed, and owns final handoff quality. No app code, UI, upload mechanics, analyzer/parser/scoring/report/privacy logic, fixtures, dependencies, deploy behavior, Playwright, or Phase 2 implementation was changed.
- 2026-05-25: Implemented ClaimGuard Agent System Upgrade 1.0 as documentation/config guidance only, with Product Strategy as the primary agent and Architecture & Maintainability, Scoring & Safety, Privacy & Evidence Safety, QA Harness, and Deployment & Release as secondary concerns. Added `ROADMAP.md` as the durable product roadmap and phase source of truth, kept `NEXT_STEPS.md` as the immediate operational queue, upgraded `AGENTS.md` to the approved 12-agent roster with phase boundaries and handoff expectations, compacted `ROUTING.md` into the agent selection table, made `REPO_SOURCE_OF_TRUTH.md` phase-aware, and aligned `.codex/commands/claimguardagent.md` plus `CLAIMGUARD_AGENT_COMMAND.md` with the expanded handoff format. No app code, UI, upload mechanics, analyzer/parser/scoring/report/privacy logic, fixtures, dependencies, deploy behavior, or Phase 2 implementation was changed.
- 2026-05-25: Ran a focused Post-Phase-1 evidence workspace refinement as the UI/Product Workflow Agent, with Scoring & Safety, Privacy & Evidence Safety, Architecture & Maintainability, and Product Strategy secondary concerns. Increased the desktop evidence/analyzer workspace height, reduced selected-evidence padding, tightened preview image padding, compacted the uploaded Evidence Intake toolbar by removing accepted-file-type copy from the uploaded state, and kept toolbar controls in the reserved band so they do not cover the receipt preview. Reframed only the visible analyzer headline for the inconclusive image case from "Unable to assess from current image" to "Manual review recommended" while leaving the underlying analyzer `reviewLabel`, report adapter, exports, scoring, and semantics untouched. Clarified the top badge as `Risk: Medium`/`Risk: <level>` and kept External Verification wording unchanged. Preserved hidden file inputs, refs, input onChange, `handleFileSelect`, `handleReset`, `handleRunAnalysis`, selected-file semantics, upload/status transition logic, Run Local Analysis behavior, Replace file behavior, Analyze Another File behavior, drag/drop handling, analyzer/parser/scoring/report/privacy logic, fixtures, real integrations, and Phase 1 scope. Verified with `npm.cmd run lint`, `npm.cmd run build`, and `npm.cmd run check:report-semantics`; no browser, Playwright, deploy, commit, real integration, or Phase 2 work was performed.
- 2026-05-25: Fixed the Evidence Intake overlay obstruction as the UI/Product Workflow Agent, with Architecture & Maintainability, Scoring & Safety, Privacy & Evidence Safety, and Product Strategy secondary concerns. Replaced the rejected floating metadata/actions overlay with a slim reserved toolbar band inside the evidence frame and moved the receipt/PDF/image viewport into a protected flex area below it, so uploaded evidence no longer renders behind translucent controls. Preserved the improved evidence-surface/corner anchoring from the partial revert by keeping the selected-state canvas in the same outer footprint, kept the duplicate completed-status pill removed, and retained only restrained micro-polish that does not affect layout mechanics. Preserved hidden file inputs, refs, input onChange, `handleFileSelect`, `handleReset`, `handleRunAnalysis`, selected-file semantics, upload/status transition logic, Run Local Analysis trigger behavior, Replace file behavior, Analyze Another File behavior, drag/drop handling, analyzer/parser/scoring/report/privacy logic, External Verification wording, fixtures, and Phase 1 scope. No browser, Playwright, deploy, commit, real integration, or Phase 2 work was performed.
- 2026-05-25: Reworked the rejected post-upload Evidence Intake header layout as the UI/Product Workflow Agent, with Architecture & Maintainability, Scoring & Safety, Privacy & Evidence Safety, and Product Strategy secondary concerns. Chose Path B, Partial revert: removed the oversized uploaded-header layout from normal document flow, kept the selected evidence canvas as the dominant surface, and moved compact Evidence Intake metadata/status/actions into a slim overlay inside the evidence preview frame so the preview no longer gets pushed down or made smaller by a large header. The selected-state scan-corner container now begins at the same evidence-surface position as the empty upload state, preserving the outlined corner footprint more closely. Kept the duplicate completed-status pill removed, and retained only restrained micro-polish that does not affect upload mechanics. Preserved hidden file inputs, refs, input onChange, `handleFileSelect`, `handleReset`, `handleRunAnalysis`, selected-file semantics, upload/status transition logic, Run Local Analysis trigger behavior, Replace file behavior, Analyze Another File behavior, drag/drop handling, analyzer/parser/scoring/report/privacy logic, External Verification wording, fixtures, and Phase 1 scope. No browser, Playwright, deploy, commit, real integration, or Phase 2 work was performed.
- 2026-05-25: Reassessed Post-Phase-1 UI Polish Pass 1 with a stricter standard as the UI/Product Workflow Agent, with Architecture & Maintainability, Scoring & Safety, Privacy & Evidence Safety, and Product Strategy secondary concerns. Chose Path A, Simplify: removed the heavier attempted continuity system from the uncommitted pass, including the evidence state badge, progress rail, and animated scan-pass treatment, because true continuity transitions need richer multi-state case-review surfaces that belong in a future Phase 3 workflow. Kept only restrained single-screen micro-polish: subtle uploaded/analyzing/completed surface state tinting, analyzer panel active/complete border treatment, completed metric emphasis, and a small result-settle reveal. Preserved hidden file inputs, refs, input onChange, `handleFileSelect`, `handleReset`, `handleRunAnalysis`, selected-file semantics, upload/status transition logic, Run Local Analysis trigger behavior, Replace file behavior, Analyze Another File behavior, drag/drop handling, analyzer/parser/scoring/report/privacy logic, External Verification wording, fixtures, and Phase 1 scope. No browser, Playwright, deploy, commit, real integration, or Phase 2 work was performed.
- 2026-05-25: Completed `hotfix-main-upload-flow` after Robert confirmed the final upload hotfix works: Choose evidence file opens the picker, selecting a local receipt/PDF imports into the app, Local evidence file selected appears, Run Local Analysis becomes available, and analysis completes with a result. Compared against the last known-good upload source at `dbe68d7:src/components/UploadPanel.tsx`, where a dedicated upload unit owned its file input/ref, triggers called that owned input, and the same input's `onChange` passed the selected file to the state flow without clearing the value during the selection event. Identified the likely regression source as the Light Evidence Lab baseline commit `d3b38cad`, which removed `UploadPanel` usage and replaced it with inline `ClaimReviewWorkflow` input/ref markup that immediately cleared `event.currentTarget.value` inside `onChange`. Reintroduced the proven UploadPanel-style architecture as a small `EvidenceFileButton` helper inside `ClaimReviewWorkflow`: each upload trigger owns its hidden input/ref/button, calls `handleFileSelect` from the real input `onChange`, and preserves the Light Evidence Lab appearance. No analyzer, parser, scoring, source classification, thresholds, fixtures, report semantics, privacy exports, customer-safe wording, External Verification semantics, raw OCR/full JSON exposure, original filenames, screenshots, customer data, private receipt data, Phase 2 behavior, icons, typography, or result-state polish changed.
- 2026-05-25: Reverted only the Receipt photo evidence-type chip from the first Nucleo camera baseline back to the original lucide camera icon as the UI/Product Workflow Agent, with Architecture & Maintainability secondary concerns. Preserved the typography/design-system pass and Light Evidence Lab recovery, removed the now-unused `NucleoCameraIcon` component and export, and left unrelated icons unchanged. No analyzer, parser, scoring, source classification, thresholds, fixtures, report semantics, privacy exports, Phase 2 behavior, customer-safe wording, External Verification semantics, private evidence exposure, raw OCR, full JSON, original filenames, screenshots, customer data, or receipt data changed. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `git diff --check`, and browser smoke for `/` and `/test-evidence`.
- 2026-05-25: Started the post-Phase-1 typography and Nucleo icon system pass as the UI/Product Workflow Agent, with Scoring & Safety and Privacy & Evidence Safety secondary concerns. Confirmed the working tree was clean after preserving prior uncommitted icon work, confirmed baseline commit `d3b38cadb02113cf40a9059ad2a98a4a3ae2a792` is present in history, and created branch `post-phase1-type-nucleo-icons`. Added SF Pro-style system typography tokens without committing font files, set the app body to 13px regular, mapped Tailwind text sizes to the approved 12/13/14/16/18/24px scale, normalized heavy typography utilities to medium weight, removed out-of-band 9/10/11px utility usage, and capped oversized display utilities so only page titles use the 24px class in the active surfaces. Added the first reusable Nucleo icon component under `src/components/icons/` with Robert's simple outline camera at 1px stroke using `currentColor`, and replaced only the "Receipt photo" chip camera icon. Inventoried remaining lucide icons and left them unchanged pending Robert-provided Nucleo code. No analyzer, parser, scoring, source classification, thresholds, fixtures, report semantics, privacy exports, Phase 2 behavior, private evidence exposure, customer-safe wording, or External Verification semantics changed. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `git diff --check`, and browser smoke for `/` and `/test-evidence`.
- 2026-05-24: Performed a v0 screenshot reference-matching pass for the recovered Light Evidence Lab main analyzer UI as the UI/Product Workflow Agent, with Scoring & Safety and Privacy & Evidence Safety secondary concerns. Kept the warm off-white page, ivory dotted board, brass scan-corner accents, gold upload action, and dark graphite analyzer panel, but tightened the page toward Robert's v0 reference: simplified the top bar to a compact mark + Phase 1 pill with the privacy pill on the right, removed the extra empty-state Evidence Intake module header, widened and calmed the evidence board, set the desktop evidence/analyzer geometry close to the reference at 1344x815 (`896x555` board and `380x555` analyzer), reduced title-section vertical drift, centered the upload stack and What ClaimGuard checks section, and reduced analyzer heading weight so the graphite panel supports rather than overpowers the board. No analyzer, parser, scoring, source-classification, thresholds, fixtures, report semantics, privacy exports, Phase 2 logic, private evidence, raw OCR, original filenames, or full JSON exposure changed. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `git diff --check`, changed-file privacy/scope scan, and browser smoke for `/` and `/test-evidence` with no console issues or horizontal overflow.
- 2026-05-24: Rechecked the deferred Light Evidence Lab theme recovery as the UI/Product Workflow Agent after Robert flagged a dark visual. Confirmed the WIP backup did contain the light theme and that `src/app/globals.css` had already recovered the warm off-white page variables, light body/main backgrounds, ivory dotted evidence surface, brass scan-frame corners, and light chips/module headers. Also confirmed the WIP intentionally kept a dark graphite analyzer result panel. Found the apparent dark-theme issue was the already-running `localhost:3000` dev server serving stale pre-recovery CSS variables; restarted that ClaimGuard dev server so `localhost:3000` now serves `--cg-bg: #f6f1e8` from the current branch. No analyzer, parser, scoring, source-classification, fixtures, privacy exports, or report semantics changed. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `git diff --check`, and browser smoke for `/` and `/test-evidence` on the refreshed server with no console issues or horizontal overflow.
- 2026-05-24: Recovered the deferred Light Evidence Lab main analyzer UI as the UI/Product Workflow Agent, with Scoring & Safety and Privacy & Evidence Safety secondary concerns. Started from clean `main` at `dbe68d7` in `C:\Users\robby\Projects\ClaimGuard`, created branch `post-phase1-light-evidence-lab-recovery`, inspected the deferred WIP backup patch, and applied only the main analyzer shell/theme recovery. Replaced the older command-center sidebar page with the evidence-first Light Evidence Lab workspace, stable evidence intake surface, polished receipt/PDF preview frame, dark graphite analyzer panel, clearer Run Local Analysis placement, collapsed analysis details drawer, and privacy-safe summary copy/export. Excluded `/test-evidence` WIP, agent icon/pipeline assets, claim-data changes, analyzer/scoring/parser changes, private evidence, raw OCR/full JSON export paths, and unrelated assets. Preserved Evidence Reliability Score semantics, External Verification: Not performed / Not externally verified language, and customer-safe wording. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `git diff --check`, privacy/file-name/raw-output scans on changed files, and browser smoke on `/` and `/test-evidence`. No commit, push, deploy, real integration, or private customer evidence was used.
- 2026-05-24: Performed final Phase 1 close verification cleanup as the Scoring & Safety Reviewer Agent. Removed exact legacy overclaiming phrases from docs/log guidance and kept the semantic smoke checker guarding the same concepts without retaining those phrases verbatim in source. Preserved Evidence Reliability Score semantics and External Verification: Not performed / Not externally verified behavior. No app behavior, analyzer thresholds, real integrations, or receipt fixtures changed. No real receipt data was requested, logged, persisted, or sent anywhere.
- 2026-05-24: Logged Phase 1 commit `0466a4a` (`test-evidence: tighten privacy-safe QA exports`) as the Privacy & Evidence Safety Agent. Scope was privacy/export cleanup only: privacy-safe fixture summary copy/export, real receipt tuning/session summary export, redacted QA observation naming, and `TEST_EVIDENCE.md` privacy workflow docs. Confirmed that no UI/agent/brand/claim-data files were committed and that no real receipt or private customer data was committed. No raw OCR or private receipt data was used in this log entry.
- 2026-05-24: Added narrow generic fallback partial-coverage fixture coverage as the Source Classification Agent. Added a synthetic generic merchant order fixture with order, product, total, and shipping cues while intentionally omitting purchase date and payment method. The fixture asserts generic fallback classification, missing purchase date/payment fields, privacy-safe presence/count-only source summaries, Amazon-validation bypass, and documents the current low-concern score/review-label behavior for future calibration without changing parser logic, scoring thresholds, review labels, UI, vendor-specific behavior, Phase 2 logic, or real receipt data. Updated the threshold note for generic partial coverage. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, a synthetic parser/scoring fixture probe, and `git diff --check` with only LF-to-CRLF warnings. No real receipt data was requested, logged, persisted, or sent anywhere.
- 2026-05-24: Implemented a narrow Home Depot cleanup as the Source Classification Agent. Added Home Depot-only parsing for partial order tokens with leading `#` while continuing to reject fulfillment/payment/total text as order IDs, paired long Home Depot total labels across short currency-label gaps, captured two-line Home Depot payment/tender/card details, and aligned Home Depot source-summary date/amount/payment cue presence with parsed fields while keeping summaries presence/count-only. Added a synthetic Home Depot long order-detail PDF fixture with a partial synthetic order token and masked payment wording, then updated Home Depot test evidence notes and tuning thresholds. Preserved Amazon, Costco, Lowe's, iSpring, Lazada, generic fallback, sparse OCR behavior, scoring thresholds, app UI, brand assets, agent visuals, and External Verification: Not performed / Not externally verified semantics. Verified with a synthetic parser probe, `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check` with only LF-to-CRLF warnings. No real receipt data was requested, logged, persisted, or sent anywhere, and no commit was performed.
- 2026-05-24: Tuned Home Depot / Home Depot Canada field extraction as the Source Classification Agent. Added source-scoped order-token extraction so blank Home Depot order labels are not misread as pickup/delivery text, added Home Depot total priority for pickup, delivery, and refund total labels, and recognized Home Depot tender/card payment rows without changing broad scoring thresholds. Added a synthetic Home Depot split-label fixture with partial synthetic order token coverage, updated test evidence docs and tuning notes, and preserved Amazon, Costco, Lowe's, iSpring, Lazada, generic merchant, unknown, sparse OCR, Evidence Reliability Score, and External Verification: Not performed / Not externally verified behavior. Verified with parser probes, `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`. No real receipt data was requested, logged, persisted, or sent anywhere, and no commit was performed.
- 2026-05-24: Added focused vendor/source identification improvements as the Source Classification Agent. Added narrow Home Depot / Home Depot Canada, Costco / Costco Canada, and Lazada order source lanes before generic merchant fallback, broadened iSpring direct order/invoice cues, added privacy-safe presence/count summaries for supported vendor order layouts, and expanded synthetic fixture coverage for those source lanes. Updated `/test-evidence` docs and threshold notes to clarify that these are local source/layout detections only, with External Verification remaining Not performed / Not externally verified. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`. No real receipt data was requested, logged, persisted, or sent anywhere, and no commit was performed.
- 2026-05-24: Clarified fixture safety wording as the Privacy & Evidence Safety Agent. Updated `AGENTS.md` guidance to use synthetic fixture/data terminology and replaced a customer-accusation example with broader intentionally-invalid-evidence wording. This keeps project instructions aligned with Phase 1 report-safety language while preserving the same privacy rule: no real customer receipt data in fixtures or commits. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`. No real receipt data was requested, logged, persisted, or sent anywhere.
- 2026-05-24: Strengthened receipt report safety checks as the Scoring & Safety Reviewer Agent. Expanded `scripts/check-report-semantics.mjs` coverage to include scoring/types, synthetic fixtures, and tuning thresholds; added guards for forbidden report/QA wording, high-score-as-proof wording, and unsafe external-verification claims; required explicit high-score-not-proof language, local-analysis-only semantics, and `externalVerification: "Not performed"`. Cleaned two older iSpring fixture notes to use synthetic wording. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`. No real receipt data was requested, logged, persisted, or sent anywhere.
- 2026-05-24: Expanded receipt analyzer fixture coverage as the Source Classification Agent. Added a synthetic Amazon print/order-details PDF fixture with print URL/title cue, Order Details heading, valid order number, date, total, payment, item, and printable-context filtering expectations. Tightened synthetic expectations so Amazon mobile action layouts assert the app-screenshot source lane and Amazon invoice/detail layouts assert the invoice/detail source lane. Updated `TEST_EVIDENCE.md` to document the Amazon print/order-detail fixture coverage. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`. No real receipt data was requested, logged, persisted, or sent anywhere.
- 2026-05-24: Improved generic merchant field extraction as the Source Classification Agent. Added broader generic date parsing for ISO, day-month-year, and no-comma month dates; recognized common split total labels such as Amount Due and Balance Due with USD/CAD-prefixed values; expanded generic payment/tender labels including Payment Type, Tender Type, Shop Pay, Apple Pay, and Google Pay; and kept Home Depot / Home Depot Canada and Costco in the generic lane because current privacy-safe evidence still does not justify narrow source categories. Added a synthetic generic split-summary fixture that verifies split date, total, payment, generic source classification, Amazon-validation bypass, and privacy-safe source summaries. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check`. No real receipt data was requested, logged, persisted, or sent anywhere.
- 2026-05-24: Tightened generic merchant source classification as the Source Classification Agent using only privacy-safe tuning patterns. Kept Generic merchant receipt as the fallback instead of adding Home Depot / Home Depot Canada or Costco source categories because the privacy-safe summary did not provide enough repeated source-specific evidence or fixture coverage for a narrow merchant parser. Replaced the single generic fallback cue with aggregate diagnostics for known generic merchant marker, receipt/invoice heading, order/transaction identifier cue, date cue, amount structure, payment/tender cue, shipping/delivery cue, and product/price line cue, with confidence derived from cue count and a capped lower confidence than supported narrow source categories. Added a privacy-safe generic parsed-field summary that reports field presence and counts only, expanded generic payment/date parsing for common labels such as split `Payment` plus card/tender detail, and added a synthetic generic order-confirmation fixture with documentation/threshold notes. Preserved Amazon, Lowe's, iSpring, sparse OCR behavior, Evidence Reliability Score semantics, and External Verification: Not performed / Not externally verified. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check` with only existing LF-to-CRLF warnings. No real receipt data was requested, logged, persisted, or sent anywhere, and no commit was performed.
- 2026-05-24: Tuned Amazon invoice/detail parsing as the Source Classification Agent. Added source-scoped Amazon invoice/order-detail date parsing for invoice/order date labels with parenthetical label text, adjacent label/value lines, weekday-prefixed dates, no-comma month dates, day-month dates, and ISO-style dates. Expanded Amazon invoice/order-detail payment parsing for `Payment Instrument`, `Payment Details`, `Paid by`, `Charged to`, credit/debit card wording, Discover/American Express cues, `ending with`, and two-line label/value payment details, while preserving Lowe's-specific logic, OCR thresholds, scoring thresholds, main app UI, agent visuals, and brand assets. Updated the synthetic Amazon invoice/detail fixture to require actual parsed purchase date and payment method extraction rather than broad cue booleans only, and updated the tuning/docs notes for the fixture behavior. Verified with a synthetic parser probe confirming visible date/payment extract and absent date/payment remain missing, plus `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and `git diff --check` with only existing CRLF warnings. No real customer receipt data was requested, logged, persisted, or sent anywhere, and no commit was performed.
- 2026-05-24: Debugged the `/test-evidence` runtime chunk-load failure as the Phase 1 Implementation Agent. Confirmed the active dev-server log showed prior Turbopack failures from `src/components/TestEvidenceHarness.tsx` compile/runtime errors (`Expression expected`, stale `realRun`, and missing `sourceClassification` access) that made `_next/static` chunk requests surface as browser 500s. The current source compiled, but the Real Receipt QA upload control still used a hidden file input nested inside a label, which made the visible upload action fragile and caused Playwright/user-facing file selection to appear dead. Replaced that nested label/input trigger with an explicit button wired to a hidden input ref, preserving browser-local analysis and unsupported-file error handling without changing analyzer scoring, thresholds, report wording, main app UI, agent visuals, or brand assets. Verified `http://localhost:3000/test-evidence`, all referenced `_next/static` chunks returning 200, synthetic JPG upload completing local analysis with no browser console errors, unsupported `.txt` upload showing a visible skipped-file error, `npm.cmd run lint`, `npm.cmd run build`, and `npm.cmd run check:report-semantics`. No real customer receipt data was logged, persisted, or sent anywhere, and no commit was performed.
- 2026-05-24: Prepared the clean `/test-evidence` harness baseline bucket as the Privacy & Evidence Safety Agent. Staged the developer-only `/test-evidence` route, the core `TestEvidenceHarness` without `AgentPipelinePanel` or agent icon dependencies, and the synthetic fixture/tuning-threshold helpers. Kept the structural redacted JSON runtime change, privacy docs/guard/log subset, agent visuals, brand assets, and main UI changes unstaged for separate buckets. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `git diff --cached --check`, and staged-harness ESLint from the index. No commit was performed.
- 2026-05-24: Made the Real Receipt QA redacted JSON export structurally privacy-safe by default as the Privacy & Evidence Safety Agent. Replaced the redacted copy path in `src/components/TestEvidenceHarness.tsx` so it now exports a `privacy-safe-redacted-diagnostic` shape built from aggregate fields instead of copying the full `analysisResult` and `finalReport` through regex masking. The redacted diagnostic omits raw OCR text, low-confidence token text, receipt raw text, parsed candidate text values, context-row values, field values, file names, last-modified timestamps, EXIF-like metadata, and raw final-report narrative fields while preserving safe diagnostics such as receipt/source class, Evidence Reliability Score, verification status, OCR quality/confidence/word counts, parsed-field presence, counts, score breakdown categories, review signal title/category/severity/confidence, and presence/count-only source summaries. Updated `TEST_EVIDENCE.md` and `scripts/check-report-semantics.mjs` to document and guard the safer redacted JSON shape. Verified with `npm.cmd run lint`, `npm.cmd run build`, and `npm.cmd run check:report-semantics`. No staging or commit was performed.
- 2026-05-23: Local checkpoint before Robert tests more real OST receipts: bucket 1 docs/source-of-truth files are staged but not committed, and the staged set should remain only `AGENTS.md`, `AGENT_LOG.md`, `REPO_SOURCE_OF_TRUTH.md`, and `ROUTING.md`. Suggested commit message remains `docs: clarify ClaimGuard agent routing and source of truth`. Remaining Phase 1 analyzer, package, `/test-evidence`, app, fixture, and brand changes should remain unstaged until reviewed by bucket. Real OST receipt examples must not be committed, stored, or added as fixtures; test them only through the `/test-evidence` Real Receipt QA browser-local flow. Prefer `Copy tuning observation` for sharing review notes, not raw OCR or full JSON.
- 2026-05-23: Staged only bucket 1 docs/source-of-truth files for commit readiness in the active repo at `C:\Users\robby\Projects\ClaimGuard`: `AGENTS.md`, `AGENT_LOG.md`, `ROUTING.md`, and `REPO_SOURCE_OF_TRUTH.md`. Reviewed those files for private customer data, raw OCR/private receipt data, incorrect OneDrive instructions, source-of-truth path accuracy, Phase 1 boundaries, customer-safe language, and the expanded CLAIMGUARD HANDOFF format before staging. Did not stage analyzer code, package files, app components, fixtures, brand assets, screenshots, PDFs, or test harness code. Did not commit.
- 2026-05-23: Reviewed the active ClaimGuard working tree in `C:\Users\robby\Projects\ClaimGuard` for commit readiness as the Architecture & Maintainability Agent. Confirmed no files were staged or committed, grouped the dirty tree into staged-commit buckets, checked for untracked receipt/customer evidence, private OCR/customer data, unsafe report wording, OneDrive path references, package-lock/package consistency, large untracked files, and LF/CRLF concerns. Found only synthetic fixture contact details, safe-language deny-list references, historical/source-of-truth OneDrive notes, and two untracked brand PNGs. Recommended not committing the full tree as one lump and flagged analyzer/scoring, `/test-evidence` exports, main workflow UI, package-lock dependency additions, and mixed line endings for manual review before staging.
- 2026-05-23: Ran a bounded Phase 1 cleanup/review loop in the confirmed active source-of-truth repo at `C:\Users\robby\Projects\ClaimGuard`. Reviewed the dirty working tree and grouped the current uncommitted work into analyzer engine, `/test-evidence` harness, source classification/fixtures, Evidence Reliability Score and verification semantics, multi-agent docs/routing, package/script changes, main analyzer UI wiring, and brand assets. Added `REPO_SOURCE_OF_TRUTH.md` with the active repo path, OneDrive duplicate warning, and a pre-commit review checklist for safely staging Phase 1 work. Did not commit. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and an in-app browser check on `http://localhost:3000/test-evidence` including the clean low-risk synthetic fixture. No analyzer behavior changed and no real customer evidence was added.
- 2026-05-23: Reconciled the ClaimGuard source-of-truth after split local work appeared between `C:\Users\robby\OneDrive\Documents\GitHub\claimguard` and `C:\Users\robby\Projects\ClaimGuard`. Confirmed `C:\Users\robby\Projects\ClaimGuard` is the active repo: it is on `main` at `origin/main`, has the Vercel project link, contains the full analyzer/test-evidence modules, and owns the running `localhost:3000` app. Confirmed the OneDrive copy is an outdated duplicate: it is 10 commits behind `origin/main`, has no Vercel project link, and contains a narrower recent wording patch. Preserved the useful OneDrive smoke-check idea by adding `npm.cmd run check:report-semantics` to the active repo with a helper adapted to the richer analyzer, report adapter, score card, `/test-evidence`, tuning exports, and docs. Kept the active repo's stronger multi-agent `AGENTS.md`/`ROUTING.md` rather than replacing them. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, and a browser check on `http://localhost:3000/test-evidence` after running the clean synthetic fixture.
- 2026-05-23: Integrated finalized ClaimGuard logo assets into the active non-OneDrive repo at `C:\Users\robby\Projects\ClaimGuard`. Added cropped transparent copies of the provided full wordmark and standalone mark under `public/brand/`, wired the full wordmark into the main Phase 1 analyzer header and sidebar brand slot, used the standalone mark for compact upload/review icon contexts and metadata icons, and removed the generic shield/check placeholder from the main analyzer brand badge. Kept the dark forensic analyzer layout and Phase 1 scope unchanged. Verified with `npm.cmd run lint`, `npm.cmd run build`, direct 200 checks for both logo paths, and an in-app browser check confirming the logo images render, the upload input/dropzone remain present, and no browser warnings/errors were reported.
- 2026-05-23: Upgraded the ClaimGuard multi-agent delegation system for Phase 1. Replaced `AGENTS.md` with a routing-first senior-agent guide covering the active Receipt Authenticity Analyzer phase, Phase 1 exclusions, global safety rules, preferred/avoided language, task quality expectations, eight specialized agents, phase boundaries, checks, direct `/claimguardagent` handling, and the expanded CLAIMGUARD HANDOFF. Added `ROUTING.md` as a quick reference with role descriptions, routing examples, phase boundaries, safe language rules, global guardrails, and handoff requirements. This was documentation-only; analyzer behavior did not change.
- 2026-05-23: Clarified Phase 1 scoring semantics so ClaimGuard treats high-scoring synthetic receipts as internally consistent evidence, not externally verified proof. Added local verification status, external-verification status, internal structure confidence, and score-meaning fields to analyzer results and report mapping. Updated the main analyzer, `/test-evidence`, privacy-safe tuning observations, fixture notes, threshold docs, and final report language to use Evidence Reliability Score / Receipt Reliability Score wording and to state that external verification was not performed. Clean synthetic fixtures can still score high because they test parser recognition, OCR readability, and structure consistency only. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scan, HTTP 200 for `/test-evidence`, and an in-app browser route load confirming reliability-score guidance appears with no fresh app console warnings/errors.
- 2026-05-23: Phase 1 autonomous loop added reusable privacy-safe source-specific parsed-field summaries, with an iSpring direct invoice implementation. The analyzer now returns `sourceSpecificSummary` with source category, structure confidence, fields-present counts, product table row count, field presence booleans, and privacy-safe notes. The iSpring summary covers order ID, status, email field, telephone field, shipping method, payment method, shipping/payment address blocks, order history, product table rows, subtotal, discount, shipping cost, and total without exposing raw private values. `/test-evidence` now renders the summary in Receipt structure, tuning observations include the same safe summary object, and the fake iSpring fixture has a manual expectation confirming the summary is present, high confidence, counts product rows, and excludes fake private values. Updated `TEST_EVIDENCE.md` with guidance for interpreting iSpring summaries as completeness/parser coverage, not authenticity proof. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scan, a browser check on production `/test-evidence` confirming the fake iSpring fixture runs `94 / Low / 92% OCR` with all six iSpring expectations passing and source summary fields visible, and a browser-local Real Receipt QA upload of the fake local iSpring PDF confirming `Copy tuning observation` includes `sourceSpecificSummary` with no raw private-value leak. No real customer receipt files were committed.
- 2026-05-23: Phase 1 autonomous loop added a synthetic fake `iSpring direct invoice` PDF fixture using non-customer data only, with iSpringFilter/iSpring Water Systems branding, fake order ID/status/email/phone/address sections, Stripe payment wording, shipping/payment addresses, product table rows, order history, totals, and footer/testimonial/newsletter/navigation copy. Improved PDF fixture generation with compact line spacing for long invoice pages so product table rows stay extractable. Tightened iSpring/non-Amazon parsing so priced product/model rows remain item evidence while private rows, addresses, emails, phones, account/navigation/footer/testimonial/newsletter/support text, free-shipping rows, order-history status rows, and tracking/carrier rows are rejected as context. Increased retained rejected line-item candidates for clearer `/test-evidence` structure explainability. Updated `TEST_EVIDENCE.md` with iSpring direct invoice QA guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scan, and a browser check on production `/test-evidence` at `http://127.0.0.1:3010/test-evidence` confirming the iSpring fixture runs `94 / Low / 92% OCR`, all five iSpring manual expectations pass, accepted item candidates are the three fake product table rows, footer/private/order-history rows are rejected, Amazon format is `not-applicable`, and no page/runtime errors were reported beyond a static 404 resource request.
- 2026-05-23: Fixed the main analyzer upload/drop behavior after the Phase 1 simplification made the upload path fragile. Replaced conditionally rendered hidden file inputs with one always-mounted `sr-only` file input, made the empty evidence zone clickable and keyboard-triggerable, moved drag enter/over/leave/drop handling onto the full evidence panel, added active drag/drop state, allowed dropping a replacement file after an upload, preserved invalid-file messaging, and kept successful uploads wired to the existing `analyzeEvidenceFile` pipeline. Added main analyzer upload manual QA steps to `TEST_EVIDENCE.md`. Verified with `npm.cmd run lint`, `npm.cmd run build`, and an in-app browser check on `/` confirming the refreshed page has one file input with the expanded image/PDF accept list, a clickable upload dropzone, no pointer-blocking overlay over the upload button/dropzone, analyzer pending state, and no browser console warnings/errors.
- 2026-05-23: Refocused the main ClaimGuard screen around the Phase 1 receipt/evidence analyzer workflow. Removed the visible sidebar, queue header, recent-case table, ticket preview, and stacked case-management surfaces from `/`; rebuilt the main page as a two-panel analyzer with a large upload/evidence preview area on the left and a compact analyzer result panel on the right. Added upload-first empty state copy, accepted evidence examples, local-only privacy copy, support-safe score/risk/confidence/recommendation output, top-three signal display, customer-safe wording, copy summary/customer response/JSON actions, and Analyze Another File controls. Moved extracted data, metadata, OCR details, signal breakdown, customer-safe response, manual review notes, export/copy JSON, and advanced debug information behind collapsed accordions. Verified with `npm.cmd run lint`, `npm.cmd run build`, and an in-app browser check at `http://127.0.0.1:3000` confirming the upload-first analyzer screen renders, old queue/dashboard labels are not visible, requested accordions are collapsed, and no browser console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved receipt source classification before scoring. Added structured source categories for Amazon app screenshots, Amazon print order details/PDFs, Amazon invoice/detail pages, iSpring direct invoices, Lowe's email/order screenshots, generic merchant receipts, and unknown/inconclusive receipts, with source confidence and matched cues. Preserved the legacy `amazon` scoring source only for Amazon categories so Amazon order-format and cue checks do not apply to iSpring, Lowe's, generic merchant, or unknown receipts. Surfaced source class, confidence, and cues in `/test-evidence` parsed fields, receipt structure, real receipt session comparison, and tuning observation export. Added safe inconclusive wording and a low-severity manual-review signal when source classification is unknown under readable OCR. Added a synthetic `Lowe's email order` fixture with fake data to verify Lowe's classification, long non-Amazon order numbers, and no Amazon-specific validation. Updated threshold visibility and `TEST_EVIDENCE.md` with source-classification guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scan, and browser checks on `/test-evidence` confirming Real Receipt QA still renders, the Amazon order page shows source classification, the Lowe's fixture runs at `84 / Low / 83% OCR`, all three Lowe's source expectations pass, Amazon format is `not-applicable`, no Amazon review signal appears for Lowe's, and no browser console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop added privacy-safe tuning observation exports to `/test-evidence` Real Receipt QA. Added per-run `Copy tuning observation` output that omits raw OCR text, original file names, parsed private values, full order IDs, payment last-four values, names, addresses, emails, phone numbers, and tracking numbers by default while preserving file category, receipt class, manual QA notes, actual risk, score, OCR quality/confidence, parsed-field presence summary, score rule/category impact, review signal titles/categories, recommendation summary, and score-fit tuning questions. Added `Copy session tuning summary` for all completed browser-memory real receipt runs using the same privacy-safe observation shape. Updated `TEST_EVIDENCE.md` to explain tuning observation vs redacted/full JSON and the recommended real receipt QA workflow. No real customer receipts or OCR contents were added. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scan, and browser checks on `/test-evidence` confirming Real Receipt QA renders, preferred tuning-observation guidance appears, the clean synthetic fixture still runs with rule/category impact and Copy result JSON, and no browser console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved the `/test-evidence` real receipt QA workflow for browser-local manual testing. Reworked Real Receipt QA from a single upload slot into a browser-memory session with multiple local uploads, active-run selection, a side-by-side comparison table for file, receipt classification, risk, score, OCR quality, score impact, manual notes, sensitive-pattern count, and status, plus per-run QA notes for expected risk, OCR accuracy, parsed-field correctness, score fit, and threshold observations. Added a privacy checklist before full JSON copying, a safer `Copy redacted JSON` path that masks obvious emails, phone numbers, Amazon-style/full numeric order IDs, payment last-four values, street-address patterns, and file names, and clearer score rule/category impact rows in the score breakdown. Updated `TEST_EVIDENCE.md` with the multi-upload real receipt workflow and redacted/full JSON guidance. No real receipt files were added or stored. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA renders with the privacy checklist and multiple-upload copy, the clean synthetic fixture still runs and shows rule/category impact, and no browser console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved Amazon multi-line section-context parsing. Added typed receipt context rows for seller, recipient/ship-to, shipping, billing, delivery/shipment, payment, invoice, and private/redacted context; surfaced those rows in `/test-evidence`; and used adjacent section labels so split `Sold by`, `Shipping address`, `Billing address`, and `Arriving` values support receipt matching without becoming item evidence. Added a synthetic `Amazon split context` fixture with split seller, recipient, billing, delivery, payment, redacted/private rows, and product detail expectations. Updated threshold visibility and `TEST_EVIDENCE.md` with guidance that names, addresses, emails, phones, masked payment details, and redacted placeholders are matching context only. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming the new fixture runs at `96 / Low / 89% OCR`, all four manual expectations pass, accepted item candidates only include `iSpring replacement filter pack`, context rows show adjacent seller/recipient/billing/delivery pairs plus private rows, and no browser console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved Amazon mobile split-date parsing. The parser now pairs common date labels with a following date line, including bare Amazon mobile labels such as `Ordered`, and records an `Adjacent order date label` source instead of treating the purchase date as missing. Added bare `ordered`/`placed` date-label handling to receipt field rejection so those rows do not become item evidence, and broadened Amazon order-placed cue detection for `Ordered` without `on`. Added a synthetic `Amazon split date` fixture with `Ordered` followed by `04/15/2026` plus manual expectations for adjacent date pairing, date-label rejection, retained Amazon proof cues, and low-risk behavior. Updated threshold visibility and `TEST_EVIDENCE.md` with split date-label guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA still renders, privacy warning remains visible, the new fixture runs at `91 / Low / 94% OCR`, all four split-date manual expectations pass, only `iSpring sediment filter pack` is accepted as item evidence while `Ordered` and `04/15/2026` are rejected context/noise, Copy JSON reaches the copied state, and no app console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved Amazon order-number OCR normalization. The parser now reconstructs conservative Amazon `3-7-7` order-number groups when OCR replaces dashes with spaces, equals signs, en/em dashes, or drops separators into a contiguous 17-digit value. Added fallback extraction for contiguous and spaced Amazon order-number patterns before applying the valid/invalid format check. Added a synthetic `Amazon OCR order number` fixture with spaced digit groups plus manual expectations for normalized order format, retained Amazon proof cues, and low-risk behavior. Updated threshold visibility and `TEST_EVIDENCE.md` with order-number OCR normalization guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA still renders, privacy warning remains visible, the new fixture normalizes to `666-6789012-4567890`, runs at `91 / Low / 95% OCR`, all three manual expectations pass, Copy JSON reaches the copied state, and no app console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved Amazon mobile split-label parsing. The receipt parser now pairs amount labels with following amount-only lines, so layouts like `Order Total` followed by `$89.99` retain labeled total source context instead of falling back to an unlabeled amount. It also pairs payment labels with following card/payment detail lines, avoids duplicate component payment candidates for those pairs, treats `Orders Total` OCR variants as order-total cues, and rejects split amount labels such as `Item subtotal`, `Estimated tax`, and `Orders Total` from item evidence. Added a synthetic `Amazon split labels` fixture with adjacent amount/payment values and manual expectations for total pairing, payment pairing, retained Amazon proof cues, and low-risk behavior. Updated threshold visibility and `TEST_EVIDENCE.md` with split label/value guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA still renders, privacy warning remains visible, the new fixture runs at `91 / Low / 89% OCR`, all four split-label manual expectations pass, split amount labels do not appear as line-item candidates, Copy JSON reaches the copied state, and no app console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved Amazon mobile/order screenshot explainability. Added rejection handling for Amazon-style mobile action/status rows such as `Buy again`, `Track package`, `Return window`, `View invoice`, product-review/support/contact actions, and delivered/arrives field labels so they do not become purchased-item evidence. Increased retained rejected line-item candidates from 8 to 12 so real receipt QA can see more rejected context in the UI and copied JSON. Added a synthetic `Amazon mobile actions` fixture with product detail mixed with delivered/status/action rows plus manual expectations for keeping product detail, rejecting navigation rows, and avoiding automatic high-risk treatment. Updated threshold visibility and `TEST_EVIDENCE.md` with Amazon mobile action-row guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA still renders, privacy warning remains visible, the new fixture runs at `81 / Low / 94% OCR`, all three Amazon mobile-action manual expectations pass, rejected `Buy again`, `Track package`, `Return window`, and `View invoice` rows appear with the navigation/action/status reason, Copy JSON reaches the copied state, and no app console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved quantity/SKU-style retail receipt parsing in `/test-evidence`. The receipt parser now normalizes accepted item candidates by stripping common SKU, UPC, item-code, and quantity prefixes after price removal, while keeping order IDs, totals, sales-tax lines, and payment cues rejected as structure/context. Added a synthetic quantity/SKU fixture with SKU, quantity, and UPC item lines plus manual expectations for prefix stripping, non-item rejection, and low-risk behavior. Updated the threshold summary and `TEST_EVIDENCE.md` with quantity/SKU guidance, and added a clipboard fallback for fixture and real-receipt JSON exports. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, privacy warning remains visible, the new fixture runs at `91 / Low / 92% OCR`, all three quantity/SKU manual expectations pass, Copy JSON reaches the copied state, and no app console warnings/errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved item-price line handling. The receipt parser now accepts product-like lines with trailing prices by stripping the amount from the item candidate while continuing to reject subtotal, tax, total, shipping, discount, refund, and payment amount lines as structure/reconciliation context. Added a synthetic item-price fixture with product+price lines plus subtotal/tax/total summaries, manual expectations for accepted product-price candidates and rejected summary lines, and threshold/docs guidance for item-price behavior. Also added a narrow malformed-OCR-fragment rejection after browser verification showed `Total: $52.12` could be read as `[Fetal S575.` and otherwise counted as item evidence. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA renders, the item-price fixture appears, accepted item candidates are `iSpring filter cartridge` and `Replacement wrench kit`, summary lines remain rejected, the malformed OCR fragment is rejected, the fixture stays `91 / Low / 87% OCR`, Copy JSON remains available, and no console errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved line-item/product-detail parsing explainability. Replaced the simple line-item filter with accepted and rejected line-item candidate review, including rejection reasons for merchant/platform headings, receipt field labels, footer/printable-page text, address/delivery text, payment text, amount lines, and non-text-like fragments. `/test-evidence` now shows rejected line-item candidates beside accepted line-item candidates so real receipt QA can verify that legal/footer/address/shipping/payment noise is not silently counted as purchased-item evidence. Added an Amazon invoice-detail manual expectation confirming `Print this page for your records` is rejected while `iSpring RCC7 replacement cartridge` remains the accepted product line, and updated the threshold summary plus `TEST_EVIDENCE.md`. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA renders, the invoice fixture remains `96 / Low / 92% OCR`, the rejected-line threshold item appears, accepted/rejected line rows render, the printable footer expectation passes, Copy JSON remains available, and no console errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved payment-method parsing and explainability for receipt tuning. Added typed payment candidates with card/wallet/gift-card/store-credit/unknown classification, selected payment source, visible-last-four detection, and payment review notes. `/test-evidence` now shows payment source, payment candidates, and payment notes so real receipt QA can distinguish split tender, gift-card/store-credit, and OCR-missing last-four cases without treating them as standalone suspicious evidence. Added a manual adjusted-receipt expectation for payment candidates and documented that payment details must be redacted before real receipt testing or JSON sharing. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA renders, the adjusted receipt remains `91 / Low / 84% OCR`, split-tender candidates render as gift-card plus card, the Amazon invoice fixture remains `96 / Low / 92% OCR`, missing-last-four payment wording is surfaced as a manual matching note, Copy JSON remains available, and no console errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved Amazon invoice/order-detail receipt parsing. Added Amazon-specific invoice/detail cues for invoice wording, sold-by, billing, and promotion/Subscribe & Save context; surfaced those cue rows in `/test-evidence`; added an Amazon invoice-detail synthetic fixture; and documented that invoice, billing, sold-by, and promotion lines are purchase-matching context rather than standalone review signals. Tightened Amazon order-number extraction to tolerate OCR dash confusions such as equals signs before validating the `000-0000000-0000000` pattern, and counted invoice date as an Amazon date cue. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the new Amazon invoice-detail fixture appears, invoice/sold-by/billing/promo cue rows render, the fixture normalizes `333=9876543=2109876` to `333-9876543-2109876`, all invoice fixture manual expectations pass, the fixture scores `96 / Low / 92% OCR`, Copy JSON remains available, and no console errors were reported.
- 2026-05-23: Phase 1 autonomous loop improved OCR confidence handling and cleaned up an over-penalized clean receipt path. The OCR service now falls back to token counts from extracted text when Tesseract/browser word metadata is missing, preventing readable high-confidence OCR text from being mislabeled as unreadable. Added a separate blocking-OCR threshold so high-average OCR with token-level uncertainty remains visible as reviewer checkpoints without automatically creating the `Receipt text is inconclusive` signal. Image-quality heuristics now use the same blocking-OCR distinction so high-average checkpoint OCR is treated as usable rather than automatically limited. Updated `/test-evidence` threshold copy and `TEST_EVIDENCE.md` with the new behavior. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA renders, the clean fixture now scores `91 / Low / 94% OCR` with no unreadable-field notes or OCR-inconclusive signal, the line-item candidate row and Copy JSON remain available, the poor-quality fixture still shows High/manual-review language, and there are no console errors.
- 2026-05-23: Phase 1 autonomous loop surfaced parsed line-item candidates for receipt tuning. The parser now includes the first readable product-like OCR lines in `parsingDetails.lineItemCandidates`, `/test-evidence` shows those candidates in the Parsed fields panel, and the copied result JSON includes them for fixture and real-receipt QA review. Updated the threshold summary and `TEST_EVIDENCE.md` so reviewers can compare candidate lines against anonymized receipt images and distinguish genuine product detail from OCR noise. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the clean fixture runs, grouped findings and parsed fields render, the line-item candidate row appears, Copy JSON reaches its copied state, and there are no console errors.
- 2026-05-23: Phase 1 autonomous loop added an Amazon multi-shipment/mobile synthetic fixture to `/test-evidence`. The fixture includes `Ordered on`, valid Amazon order number, `Order Summary`, `Shipment 1 of 2`, `Arriving`, item detail, shipped-to wording, payment method, subtotal/shipping/tax, and order total so multi-shipment context can be reviewed separately from ordinary Amazon order-page and suspicious invalid-order fixtures. Added manual expectations for multi-shipment cues, retained proof cues, and avoiding automatic high-risk treatment. Updated `TEST_EVIDENCE.md` to document the expanded Amazon fixture coverage. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the multi-shipment fixture card appears, the fixture runs, manual expectations render, Amazon shipment cue rows render, multi-shipment context appears, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop improved Amazon mobile/order-page structure cue parsing. Added Amazon-specific cues for items ordered, order summary, arrival/shipment wording, and multi-shipment wording; broadened ship/delivery detection to include shipped-to, arriving, arrives, shipment, and similar mobile-order labels; and expanded Amazon total/summary amount matching. Surfaced the new Amazon cue rows in `/test-evidence` and updated threshold visibility plus `TEST_EVIDENCE.md` so these cues are treated as proof-of-purchase matching context, not customer accusations. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the Amazon order-page fixture runs, new Amazon cue rows render, manual expectations remain visible, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop added a readable Amazon order-page synthetic fixture to `/test-evidence`. The new fixture uses a valid Amazon-style order number plus order placed/date, Items Ordered, ship-to, payment method, subtotal/shipping/tax, order total, and delivered cues so Amazon baseline behavior can be tuned separately from the suspicious invalid-order fixture. Added manual expectations for valid Amazon order format, expected Amazon structure cues, and avoiding automatic high-risk treatment. Updated `TEST_EVIDENCE.md` to document the readable Amazon baseline. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the Amazon order-page fixture card appears, the fixture runs, manual expectations render, Amazon cue rows render, valid order format is visible, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop added an adjusted-payment synthetic fixture to `/test-evidence`. The new adjusted receipt fixture includes discount, return-credit, gift-card, and split-payment-style lines so manual QA can verify amount categories and reconciliation notes without storing real customer evidence or making expectations CI-blocking. Added manual expectations that check reconciliation notes appear and that adjusted-payment layouts do not automatically become high risk. Updated `TEST_EVIDENCE.md` to document the expanded synthetic fixture set. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the adjusted fixture card appears, the fixture runs, manual expectation labels render, amount categories and amount notes render, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop added safe amount reconciliation notes for payment-adjustment receipt layouts. The parser now generates amount review notes for discounts/coupons/savings, refunds/return credits, multiple payment amount lines, gift-card/store-credit/split-payment/partial-payment wording, and multiple total-like amounts. These notes are included in receipt structure notes and surfaced in `/test-evidence` as "Amount notes" so reviewers treat them as order-matching context rather than standalone suspicious findings. Updated threshold visibility and `TEST_EVIDENCE.md` to reinforce that payment-adjustment layouts should be reconciled against order records. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the clean fixture runs, amount notes render, amount categories remain visible, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop added structured amount-category parsing for receipt tuning. Amount candidates now include categories for subtotal, tax, shipping, discount, refund, total, payment, and other; parser structure detection can use those categories for subtotal/tax/shipping context, while total selection still prefers total-like labels before using fallback amounts. `/test-evidence` now shows amount candidates with category labels plus an amount-category summary, and docs/threshold visibility explain how categories help review multi-total receipts without treating every amount difference as suspicious. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the clean fixture runs, subtotal/tax/total categories render, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop improved merchant receipt date/amount parsing and tuning explainability. Broadened purchase-date extraction for order, purchase, transaction, invoice, receipt, and generic date labels; replaced first-currency fallback total selection with line-based amount candidates that prefer grand/order total, total paid, amount paid, payment total, charged, and other total-like labels before falling back to the last visible amount. Added parsing details for purchase-date source, selected-total source, and amount candidates, surfaced them in `/test-evidence`, and updated threshold visibility plus `TEST_EVIDENCE.md` so Real Receipt QA reviewers can understand why a date or total was selected. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the clean fixture runs, date/total source rows render, amount candidates render, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop improved low-confidence OCR reporting for manual receipt QA. Added typed OCR uncertainty notes that group sparse/unreadable OCR, very low-confidence tokens, low-confidence checkpoints, and possible parsed-field impacts while preserving raw low-confidence samples. `/test-evidence` now shows OCR uncertainty notes before raw word chips so reviewers can quickly understand whether poor OCR may affect amounts, dates, order numbers, or payment details. Updated threshold visibility and `TEST_EVIDENCE.md` to explain the new OCR uncertainty notes and reinforce that they are manual-review aids, not alteration findings. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the poor-quality fixture runs, OCR uncertainty notes render, low-confidence samples remain visible, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop improved field-level OCR reliability reporting. Added typed receipt field reliability entries for merchant, order number, purchase date, line item/product detail, total, and payment method; parser confidence is now adjusted by OCR quality so extracted values can still be marked "Needs review" when OCR is usable, inconclusive, or unreadable. Surfaced the field reliability notes in `/test-evidence` beside parsed fields, and updated threshold visibility plus `TEST_EVIDENCE.md` to explain how reviewers should use those notes during Real Receipt QA. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the poor-quality fixture runs, field OCR reliability appears with weak-OCR language, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop improved score breakdown explainability for real receipt tuning. Added typed score details for OCR confidence penalty, OCR quality-label penalty, low-confidence rate, credited parsed fields, missing-field count, and the parsed-field bonus rule. Updated scoring to compute confidence and quality penalties separately while preserving the same risk-band behavior. Surfaced these details in `/test-evidence` under Score breakdown, and updated the threshold summary plus `TEST_EVIDENCE.md` so reviewers can tell whether a result was too harsh because of OCR confidence, OCR quality label, missing fields, or review-signal penalties. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, a clean synthetic fixture runs, score-detail rows render, Copy JSON remains available, and the final report renders.
- 2026-05-23: Phase 1 autonomous loop improved Amazon receipt parsing and structure validation. Tightened order-number extraction so bare `Order` text is less likely to be misread as an order number, added support for spaced Amazon order numbers, Amazon order placed/date labels, Amazon payment-method labels, and expanded shipping/delivery cues. Added typed Amazon structure cues for order placed/date, order total, ship/delivery context, payment cue, and order-number issue; scoring can now raise a safe "Amazon receipt structure needs verification" signal when readable Amazon-like receipts are missing expected proof-of-purchase cues. Surfaced the Amazon cue rows in `/test-evidence`, updated threshold visibility and docs, and preserved customer-safe wording. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser check on `/test-evidence` confirming Real Receipt QA still renders, the suspicious Amazon-style fixture runs, detailed output appears, Amazon cue rows render, Copy JSON remains available, and the final report renders.
- 2026-05-23: Verified the Phase 1 `/test-evidence` real-world receipt QA workflow. Confirmed the developer-only harness includes temporary browser-local real receipt upload for PNG, JPG, WEBP, and PDF files; detailed analyzer output for OCR text, parsed fields, metadata, image quality, score breakdown, review signals, recommendation, final report, and result JSON copy; local-only QA notes for expected risk, OCR accuracy, parsed-field correctness, score fit, and threshold suggestions; and privacy guidance to anonymize real receipts and avoid committing customer evidence. Updated verification status with `npm.cmd run lint`, `npm.cmd run build`, and an in-app browser check on `/test-evidence` confirming Real Receipt QA renders and a synthetic fixture still produces detailed output, manual expectation labels, copy JSON, and the final report.
- 2026-05-23: Phase 1 autonomous loop improved metadata and image-quality signal usefulness. Added metadata context status/summary so missing EXIF or limited PDF context is treated as weak source context, not a standalone suspicious finding. Split image heuristics into quality level/summary, evidence-quality indicators, source-context indicators, and narrower potential alteration indicators. Retuned scoring signal wording so metadata says "context only" and image compression/low resolution are evidence-quality limits that may justify a clearer copy. Surfaced metadata context, image quality level, source-context notes, evidence-quality notes, and potential indicators in `/test-evidence`; updated threshold summary and docs with the new separation. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA, privacy language, metadata context thresholds, quality labels, grouped findings, metadata findings, and image-quality findings render.
- 2026-05-23: Phase 1 autonomous loop added grouped analyzer findings for clearer support review. Added typed `findingGroups` to local analysis results with `OCR/Text`, `Receipt Structure`, `Metadata`, `Image Quality`, and `Recommendation` categories; each group now carries a status, summary, detail rows, and related signal ids. The report adapter now uses these groups for final verification checks, and `/test-evidence` shows a grouped findings panel before the detailed OCR/structure/metadata/image/score sections. Updated docs to explain using grouped findings before individual signals so inconclusive quality issues are not confused with potential alteration indicators. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA, privacy language, grouped findings, all five finding categories, and the final report render.
- 2026-05-23: Phase 1 autonomous loop improved OCR confidence handling and low-quality evidence explainability. Added OCR quality labels (`Clear`, `Usable`, `Inconclusive`, `Unreadable`) based on average confidence, text volume, and low-confidence word rate; added OCR quality summaries to analyzer output; adjusted scoring to include explicit OCR quality penalties; changed support/customer wording so inconclusive or unreadable OCR asks for clearer proof rather than implying suspicious alteration; surfaced OCR quality, word counts, low-confidence rate, and low-confidence word samples in `/test-evidence`; updated threshold summary and docs with OCR quality guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming OCR quality thresholds, Real Receipt QA privacy language, poor-quality fixture output, low-confidence samples, customer-safe recommendation, and Copy JSON render.
- 2026-05-23: Phase 1 autonomous loop improved analyzer structure handling and explanation. Added receipt-structure parsing for line items, subtotal/tax/shipping cues, currency amount count, normalized Amazon order numbers, and Amazon order-format notes; changed low-OCR signals to clearly describe inconclusive evidence quality rather than suspected alteration; added a separate receipt-structure review signal for readable receipts with weak structure; expanded score breakdown with formula, risk bands, and interpretation; surfaced receipt structure in `/test-evidence`; updated threshold summary and docs to explain structure thresholds and the low-OCR/suspicious separation. Verified with `npm.cmd run lint`, `npm.cmd run build`, and browser checks on `/test-evidence` confirming Real Receipt QA, privacy warning, receipt structure, score formula, interpretation, and customer-safe recommendation render.
- 2026-05-23: Began replacing the mock-only upload analysis path with a real local receipt/evidence analyzer while preserving the current workstation UI. Added browser-side OCR/PDF text extraction, receipt field parsing, metadata inspection, image-quality heuristics, typed local analysis results, scoring, dynamic review signals, and a report adapter that feeds the existing score/signals/ticket panels. Added `tesseract.js`, `pdfjs-dist`, and `exifr` for local-only analysis. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scan, and a local route load check returning HTTP 200 on `http://localhost:3000`. Remaining work: automate browser upload-flow verification with sample receipt fixtures and refine OCR performance/error handling.
- 2026-05-23: Used the attached dashboard mockup as visual direction and simplified the ClaimGuard interface around a clearer evidence-review hierarchy. Removed the large top product hero from the active page; rebuilt the left sidebar into grouped Investigation, Intelligence, Automation, and Settings navigation; made the case header calmer with Back to Queue, case ID, Needs Review status, source, submitted time, channel, reviewer, and case actions; made the evidence viewer the dominant central surface with thumbnails, scan frame, ruler ticks, zoom controls, and document-detected status; compacted the right risk panel and detected signals; reduced support context to two lower panels for Case Context and Customer Message; and simplified recent cases into a small evidence queue. Updated `DESIGN.md` to capture this mockup-aligned direction. Verified with `npm.cmd run lint`, `npm.cmd run build`, browser check on `http://127.0.0.1:3000`, and support-safe wording scan.
- 2026-05-23: Applied a second ClaimGuard design polish pass focused on product identity and evidence-review realism. Made the evidence viewer the dominant workstation surface with scan-frame styling, ruler ticks, zoom controls, evidence ID, upload timestamp, file metadata, and hash-style details; added an investigation record header with case status, review queue, reviewer, channel, last updated, and evidence type; upgraded the support ticket panel with customer message, order number, claim reason, purchase channel, uploaded attachments, and support rep notes; redesigned risk intelligence around evidence confidence, signal count, severity distribution, review state, and recommended support action; expanded detected signals with severity, confidence, evidence source, explanation, and manual review recommendation; and made the recent case table read more like an evidence queue. Updated `DESIGN.md`, `AGENTS.md`, and mock case metadata without adding real integrations. Verified with `npm.cmd run lint`, `npm.cmd run build`, a browser load check with no console warnings/errors, and a support-safe wording scan.
- 2026-05-23: Created `DESIGN.md` and applied the ClaimGuard forensic command-center visual system across the actual app. Updated `AGENTS.md` with design-system rules; replaced the pale centered MVP surface with a dark evidence-review shell; restored rendered sidebar, ticket preview, risk score, review signals, analysis report, and recent-case queue; added deep navy/charcoal surfaces, subtle grid/scanline texture, receipt-paper ticket panels, electric-blue and verification-green accents, security-style badges, asymmetric command-center layout, and an unsupported-file error state in the upload panel. Verified with `npm.cmd run lint`, `npm.cmd run build`, browser load check at `http://127.0.0.1:3000` with no console warnings/errors, visible command-center/evidence-review copy, and support-safe wording scan.
- 2026-05-23: Repaired the autonomous ClaimGuard agent setup after the old OneDrive workspace path became empty. Located the active repo at `C:\Users\robby\Projects\ClaimGuard`, updated the `claimguard-project-agent` heartbeat prompt and local `claimguardagent` skill to use that path, added the required ChatGPT handoff section to `AGENTS.md`, cleaned the completed handoff request out of `AGENT_INBOX.md`, and confirmed no stale `C:\Users\robby\OneDrive\Documents\ClaimGuard` references remain in the repo, skill, or automation config. No app code changed, so lint/build were not run.
- 2026-05-23: Applied a premium visual pass to the simplified ClaimGuard MVP without changing core layout or behavior. Upgraded the top brand header into a polished glassy band, strengthened the shared card treatment with softer layered shadows and larger rounded surfaces, refined the upload dropzone with stronger hover/focus affordances and selected-file surfaces, improved the Evidence Reliability Score presentation with a branded gradient ring and polished risk badges, added a subtle non-busy background grid/gradient, and preserved upload/analyze/re-run behavior, KB file sizes, key signals, manual review note, and collapsed review guidance. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe/no-dashboard-clutter scans, and a local browser check on port 3000 confirming the premium header, larger aligned cards, no sidebar/dashboard noise, and no app console warnings or errors.
- 2026-05-23: Reworked the simplified ClaimGuard MVP into a focused single-purpose upload tool without adding real integrations or bringing back dashboard noise. Removed the active dark sidebar from the page, moved the ClaimGuard logo into a clean top header beside the headline/subtitle, expanded the upload and authenticity cards into a centered max-width two-column workflow, added a lighter logo-color background gradient, made the empty authenticity state more intentional, and preserved upload/analyze/re-run behavior, KB file sizes, and support-safe wording. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe/no-dashboard-noise scans, and a local browser check on port 3000 confirming no sidebar, no metric/ticket/queue labels, larger centered cards, and no app console warnings or errors.
- 2026-05-23: Tightened the simplified ClaimGuard MVP dashboard balance without changing upload/analyze/report behavior or adding integrations. Shortened the headline to "Check evidence authenticity"; narrowed the sidebar to a compact single-item rail; reduced the two-card workflow width and spacing so upload and result sit closer and higher; changed visible review labels to "Evidence review"; and gave the completed authenticity result more visual weight with a larger score treatment and subtle brand accent. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe wording scans, and a local browser check on port 3000 confirming the shorter headline, compact sidebar, tighter card gap, updated result label, and no app console warnings or errors.
- 2026-05-23: Simplified the ClaimGuard dashboard down to the core upload-to-authenticity MVP without adding real integrations. Removed the main-screen metric cards, ticket preview, current review case, recent cases table, selected-case workflow, secondary sidebar nav emphasis, and large report stack from the active dashboard path; reduced the sidebar to logo plus Dashboard; centered a two-card layout with upload on the left and a compact authenticity result on the right; preserved the existing local mock upload/analyze/report state machine, KB file-size display, safer report wording, and completed-state re-run button copy. Verified with `npm.cmd run lint`, `npm.cmd run build`, support-safe/noise wording scans, and a local browser check on port 3000 confirming the simplified dashboard renders with no app console warnings or errors.
- 2026-05-23: Cleaned up remaining ClaimGuard demo wording and file-size display issues without changing layout or adding integrations. Ensured completed-report upload actions show "Re-run mock analysis" or "Run another review"; kept selected upload sizes routed through the KB-under-1-MB formatter; changed the sub-1-MB mock PDF case from `0.8 MB` to `819 KB`; and replaced remaining PDF "consistent" language with safer mock-review wording. Verified with `npm.cmd run lint`, `npm.cmd run build`, a support-safe wording scan, and a local browser check on port 3000 confirming `819 KB`, safer PDF report text, no old demo wording, and no app console warnings or errors.
- 2026-05-23: Refined the ClaimGuard dashboard UI after the branding pass without changing the mock upload/analyze/report behavior or adding real integrations. Removed the redundant header logo, softened the app shell, sidebar active state, metric cards, upload area, panels, tables, and report cards; added a controlled cyan-to-green primary button with a clearer disabled state; changed completed upload action copy to "Re-run mock analysis"; added KB display for files under 1 MB; and replaced overconfident low-risk report wording with support-safe mock-review language. Verified with `npm.cmd run lint`, `npm.cmd run build`, a support-safe wording scan, and a local browser check on port 3000 with no app console warnings or errors.
- 2026-05-23: Performed a ClaimGuard branding pass without adding real integrations. Added the exact uploaded logo from `C:\Users\robby\OneDrive\Documents\Flow Desk\ClaimGuard Logo.png` as `public/claimguard-logo.png`; wired that PNG into the sidebar and header; updated sidebar, header, upload workflow, score card, selected case state, case table, report cards, badges, and review panels to use the logo-aligned navy, cyan, teal, green, soft-card, and light-blue-border palette. Verified with `npm.cmd run lint`, `npm.cmd run build`, a support-safe wording scan, and a local browser check on port 3000 with no app console warnings or errors.
- 2026-05-23: Added selectable recent-case review behavior and simplified the ClaimGuard dashboard into a cleaner support workspace. Recent case rows now reopen full mock case reports with matching ticket preview, score, risk level, evidence summary, grouped signals, verification checks, and customer-safe response guidance; newly analyzed mock cases still appear at the top of the queue. Polished the upload card, current-case label, score/signals panel, report readability, sidebar, and iSpring-style mock warranty content. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a local browser check on port 3001 with no console warnings or errors.
- 2026-05-23: Added repo-level agent communication files, configured the ClaimGuard Project Agent automation, and upgraded the MVP upload-to-analysis workflow with local mock states and support-safe report outputs. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a browser load check with no console warnings or errors.
- 2026-05-23: Improved the current upload-to-analysis MVP flow without adding real integrations. Added clearer upload/analyze/report step cues, richer ticket preview details, stronger authenticity-score explanation, completed report primary finding and verification checks, and renamed visible risk output toward review signals. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a local browser load check with no console warnings or errors.
- 2026-05-23: Added the `/claimguardagent` project command definition, user-facing command instructions, and a PowerShell helper for adding direct agent tasks to `AGENT_INBOX.md`. Verified command references and helper usage output.
- 2026-05-23: Added a local Codex `claimguardagent` skill so future sessions can recognize direct ClaimGuard agent tasks. The bundled skill validator could not run because its Python environment is missing `yaml`; manual frontmatter and file checks were used instead.
- 2026-05-23: Upgraded the completed mock analysis report output into a fuller support-review artifact. Added score explanations, confidence levels, categorized risk signals, support rep recommendations, customer-safe response suggestions, verification checklists, signal-versus-proof guardrails, and a manual-review-only disclaimer. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a local browser load check with no console warnings or errors.
- 2026-05-23: Clarified `/claimguardagent` semantics: direct command tasks wake the agent and run immediately in the current thread, and should not be duplicated into `AGENT_INBOX.md`. The inbox and helper script remain only for future queued tasks.
- 2026-05-23: Upgraded the Phase 1 upload-to-analysis demo flow without adding real integrations. Added richer selected-file metadata, ready-to-analyze state, a five-step mock analysis sequence, ticket preview evidence/result updates, and a highlighted newly analyzed case row in recent cases. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a local browser load check with no console warnings or errors.
- 2026-05-23: Fixed upload event/state wiring for the demo flow. Replaced the fragile label-only upload trigger with an explicit ref-backed upload button, kept the file input `onChange` wired to parent state, added drag/drop handling, reset the native input when clearing evidence, and confirmed the JPG upload path enables mock analysis and completes the report in Playwright. Robert waived additional PNG/PDF manual verification for this pass. Verified with `npm.cmd run lint`, `npm.cmd run build`, and a local app load check.
- Later task: Tune the mock analysis outputs so receipt, screenshot, PDF, and product-photo reports feel more varied and realistic while staying mock-only and support-safe. Do not connect real AI yet.
- 2026-05-30: Phase 2.4.10 — implemented the no-live `pre-analysis-evidence-gate` contract/probe planned in Phase 2.4.9. Added `src/lib/analysis/pre-analysis-evidence-gate.ts` (decision-only, fully self-contained, `@/`-free): exports `PRE_ANALYSIS_EVIDENCE_GATE_STATUS` and `buildPreAnalysisEvidenceGateDecision`, accepting synthetic filename/type/category/boolean hints only and returning one of `allow-receipt-default-path`, `unsupported-evidence`, `legacy-damage-photo-quarantine`, `product-photo-like-unsupported`, or `unknown-inconclusive` with explicit no-live markers (`runtimeLive: false`, `manualReviewOnly: true`, and ocr/metadata/analyzer/adapter/upload-UI-report-scoring-parser-fixture/provider-storage-integration-case-queue invoked flags all false). Receipt/PDF/screenshot/order hints preserve the existing receipt/default path; product-photo-like and legacy damage-photo hints are stopped non-live; reasons are generic so raw filenames are never echoed. Added `src/lib/analysis/pre-analysis-evidence-gate.probe.ts` (`PRE_ANALYSIS_EVIDENCE_GATE_DEVELOPER_PROBE`) covering all outcomes, marker assertions, raw-filename/private-key leak checks, unsafe-wording checks, compile-time forbidden-input-key assertions, and import-boundary scans; registered it in `scripts/run-product-photo-probes.cjs` (now 11 modules) and added gate/probe marker, import, privacy, and damage-photo-runtime-leak guards to `scripts/check-report-semantics.mjs`. The gate is not wired into `analyzeEvidenceFile`, upload, UI, the live report adapter, the receipt-shaped result, or runtime; no OCR/metadata/provider/storage/integration/case-queue work was added; receipt behavior and `damage-photo` legacy/non-canonical status are unchanged. Verified with `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run check:report-semantics`, `npm.cmd run check:product-photo-probes`, `git diff --check`, and targeted import/boundary scans. Committed but not pushed.
- 2026-05-30: Phase 2.4.10 push checkpoint — pushed commit `f5564b4` (`feat: add pre-analysis evidence gate contract`) to `origin/main`; `main` clean and synced. No files edited, no implementation work, no deploy.
- 2026-05-30: Phase 2.4.11 — docs-only source-of-truth alignment. Updated `REPO_SOURCE_OF_TRUTH.md` so its Phase-Aware Workflow and stop conditions no longer describe a stale "Phase 2.0 scaffold-only / Phase 2.1 not yet started" state. It now records that Phase 2.4 adapter-readiness/runtime-blocker planning is complete, the guarded non-live product-photo adapter contract and the decision-only unwired pre-analysis evidence gate exist and are pushed (`f5564b4`), product-photo runtime is non-live, `damage-photo` is legacy/non-canonical, `analyzeEvidenceFile` is the live receipt entrypoint, `LocalAnalysisResult`/receipt behavior are unchanged, UI/upload/live report adapter/provider/storage/integration/case-queue work remains blocked, dev-only harness work is a future no-live milestone, and Phase 2.4 closeout review is next. Added a matching Phase 2.4.11 checkpoint bullet to `NEXT_STEPS.md`. Docs-only; no code, route, component, runtime, analyzer, parser, scoring, fixture, provider, storage, integration, or case-queue changes. Verified with lint, build, report-semantics, product-photo-probes, and `git diff --check`. Committed but not pushed.
- 2026-05-30: Phase 2.4.12 review-only Phase 2.4 closeout — confirmed Phase 2.4 complete as a non-live adapter/readiness/runtime-blocker phase. Verified product-photo runtime non-live (`ENABLE_PRODUCT_PHOTO_RUNTIME_ROUTING = false`), pre-analysis evidence gate decision-only/unwired, `analyzeEvidenceFile` receipt-shaped and called only by the live receipt workflow, `LocalAnalysisResult` receipt-shaped, receipt behavior unchanged, no provider/storage/integration/case-queue work, classifier never returns `damage-photo`, canonical product-photo analyzer free of `damage-photo`. lint/build/report-semantics/product-photo-probes (11 modules)/diff-check all passed. No files changed.
- 2026-05-30: Phase 2.4.13 — tiny docs-only AGENTS.md phase-state alignment. Updated the "Current Product State" section and the Phase 2 line in the Phase Boundaries summary so future agents no longer inherit stale "Phase 2.1 documentation/planning-only" wording. AGENTS.md now records Phase 2.4 closed (non-live adapter/readiness/runtime-blocker phase), product-photo runtime non-live, guarded non-live product-photo adapter contract exists, pre-analysis evidence gate decision-only/unwired, `damage-photo` legacy/non-canonical, `analyzeEvidenceFile` the live receipt entrypoint, `LocalAnalysisResult` receipt-shaped, receipt behavior unchanged, UI/upload/live report adapter/provider/storage/integration/case-queue work blocked, and the dev-only adapter review harness as the next likely no-live milestone. Agent roles, hierarchy, and architecture instructions were left unchanged. Docs-only; verified with report-semantics, product-photo-probes, and `git diff --check`. Committed but not pushed.
- 2026-05-30: Phase 2.4.13 push checkpoint — pushed commit `ecc45cd` (`docs: align agents phase 2.4 status`) to `origin/main`; `main` clean and synced. No files edited, no implementation, no deploy.
- 2026-05-30: Phase 2.5.0 — docs-only Phase 2.5 scope plan. Added `PHASE_2_5_DEV_HARNESS_SCOPE_PLAN.md` defining Phase 2.5 as the dev-only adapter/gate review harness phase: a no-live, synthetic-only, unlinked, production-disabled developer surface for inspecting product-photo adapter readiness outputs and `pre-analysis-evidence-gate` decisions. It specifies what Phase 2.5 is/is not, the harness scope, required synthetic cases (adapter readiness + gate outcomes + hostile/sentinel-omission + long-text stress + manual-review-only no-live marker), allowed future files, protected files, semantic/probe/browser/privacy gates, stop conditions, done criteria, and recommends Phase 2.5.1 as a gate-first no-live dev-only review harness implementation (or a review-only closeout). Updated `ROADMAP.md` Phase 2 status (2.4 closed incl. 2.4.10 gate, Phase 2.5 = dev harness scope), added a Phase 2.5.0 checkpoint and Next Safe Task to `NEXT_STEPS.md`, and added a supersede pointer atop `PRODUCT_PHOTO_DEV_HARNESS_PLAN.md`. Docs-only; no code, route, component, runtime, analyzer, parser, scoring, fixture, provider, storage, integration, or case-queue changes. Product-photo runtime remains non-live; pre-analysis evidence gate remains decision-only and unwired. Verified with report-semantics, product-photo-probes (11 modules), and `git diff --check`. Committed but not pushed.
- 2026-05-30: Phase 2.5.0 push checkpoint — pushed commit `e87b3b1` (`docs: plan phase 2.5 dev harness scope`) to `origin/main`; `main` clean and synced.
- 2026-05-30: Phase 2.5.1 — gate-first no-live dev-only review harness implementation. Added `src/app/dev/pre-analysis-evidence-gate/render-cases.ts` (type-only import of `PreAnalysisEvidenceGateDecision`; 10 literal synthetic review cases: receipt/PDF/order-screenshot allow, product-photo-like unsupported, legacy damage-photo quarantine, unsupported media-type, unknown inconclusive, hostile sentinel-omission, long-text layout stress, manual-review-only no-live marker) and `src/app/dev/pre-analysis-evidence-gate/page.tsx` (server component, production-disabled via `notFound()` when `NODE_ENV === "production"`, unlinked, dark forensic styling, non-live banner, renders outcome/allow-path/no-live isolation markers/reasons/legacy-compatibility/limitations with visually distinct outcome badges). The gate builder is not value-imported or called; no analyzer/OCR/metadata/upload/report-adapter/provider/storage/integration/case-queue imports; no files, blobs, object URLs, images, fetch, or browser storage. Extended `scripts/check-report-semantics.mjs` with gate-host required-signal, forbidden-import, forbidden-pattern, and live-file no-link guards. Probe runner unchanged (still 11 modules). Browser QA: dev server load of `/dev/pre-analysis-evidence-gate` returned HTTP 200 with the non-live banner, "Runtime live: No", "Manual review only", all five distinct outcome labels, and no `<img>`/file inputs; `/` returned 200 and does not link the dev route. Product-photo runtime stays non-live; pre-analysis evidence gate stays decision-only and unwired; `analyzeEvidenceFile`/`LocalAnalysisResult`/receipt behavior unchanged. Verified with lint, build, report-semantics, product-photo-probes, diff-check, and targeted import/privacy scans. Committed but not pushed.
- 2026-05-30: Phase 2.5.2 push checkpoint — `84ad4a7` (`feat: add dev product photo adapter readiness harness`) confirmed on `origin/main` (already pushed at prior step); `main` clean and synced.
- 2026-05-30: Phase 2.5.3 review-only Phase 2.5 closeout — confirmed both dev harnesses (`/dev/pre-analysis-evidence-gate`, `/dev/product-photo-adapter-readiness`) are no-live, synthetic-only, unlinked, production-disabled, type-only-import, with no builder calls/value imports and no `analyzeEvidenceFile`/`LocalAnalysisResult`/media/upload coupling. Phase 2.5 commits (`e87b3b1^..84ad4a7`) touched only docs, the semantic guard, and the four dev-route files (no protected files). lint/build/report-semantics/product-photo-probes (11 modules)/diff-check passed; dev-route QA passed (both routes HTTP 200 with distinct outcomes, `/` unlinked, no img/file inputs). No files changed. Phase 2.5 closed.
- 2026-05-30: Phase 2.6.0 — docs-only runtime-facing scope plan. Added `PHASE_2_6_RUNTIME_SCOPE_PLAN.md` defining Phase 2.6 as the first runtime-facing planning phase (no implementation), documenting the live `analyzeEvidenceFile` pipeline, and recommending a guard-only `pre-analysis-evidence-gate` wiring (block/quarantine product-photo-like and unsupported files before OCR/metadata; no product-photo analysis; additive unsupported result that avoids `LocalAnalysisResult` migration; default-off flag; receipt-preservation probes) as the safest eventual first runtime slice, preceded by a docs-only Phase 2.6.1 design spike. Covers sequence options A–E, prerequisites, shared-result/`LocalAnalysisResult` decision, upload/UI/report sequencing, privacy/data-flow gates, required checks, stop conditions, and the Phase 2.6.1 recommendation. Added pointers to `ROADMAP.md`, `NEXT_STEPS.md`, and `PRODUCT_PHOTO_RUNTIME_BLOCKERS_PLAN.md`. Docs-only; no code, route, component, runtime, analyzer, parser, scoring, fixture, provider, storage, integration, or case-queue changes. Product-photo runtime stays non-live; gate/adapter stay decision-only and unwired. Verified with report-semantics, product-photo-probes, and `git diff --check`. Committed but not pushed.
- 2026-05-30: Phase 2.5.1 push checkpoint — pushed commit `bf284e0` (`feat: add dev pre-analysis gate harness`) to `origin/main`; `main` clean and synced.
- 2026-05-30: Phase 2.5.2 — no-live dev-only product-photo adapter-readiness review harness. Added `src/app/dev/product-photo-adapter-readiness/render-cases.ts` (type-only import of `ProductPhotoAdapterReadinessResult`; 12 literal synthetic cases: accepted analysis-result, accepted report-view-model, top-level damage-photo quarantine, nested analysis-result damage-photo quarantine, nested report-view-model damage-photo quarantine, receipt-like/unknown/unsupported mismatch collapse, hostile sentinel-omission, limited metadata bucket, long-text layout stress, manual-review-only no-live marker) and `src/app/dev/product-photo-adapter-readiness/page.tsx` (server component, production-disabled via `notFound()`, unlinked, dark forensic styling, non-live banner, renders readinessAccepted/inputKind/evidenceType/sourceKind/confidence/reviewPriority/localSignalLevel/score/summary/action/customer-wording/legacy-compatibility/signals/no-live isolation markers/privacy posture/limitations with distinct accepted/quarantine/unsupported badges). The readiness builder is not value-imported or called; no analyzer/OCR/metadata/upload/report-adapter/provider/storage/integration/case-queue value imports; no files/blobs/object URLs/images/fetch/storage. Extended `scripts/check-report-semantics.mjs` with adapter-host required-signal, forbidden-import, forbidden-pattern (builder/`analyzeEvidenceFile(`/value-import/image/file/fetch/storage/IDs/URLs), and live-file no-link guards; the type-only readiness import is allowed. Probe runner unchanged (11 modules). Browser QA: dev server load of `/dev/product-photo-adapter-readiness` returned HTTP 200 with the non-live banner, "Runtime live: No", "Manual review only", distinct Readiness accepted / Legacy quarantine / Unsupported collapse badges, isolation markers, and privacy posture; no `<img>`/file inputs; `/` returned 200 and does not link the dev route. Product-photo runtime stays non-live; adapter readiness stays decision-only and unwired; `analyzeEvidenceFile`/`LocalAnalysisResult`/receipt behavior unchanged. Verified with lint, build, report-semantics, product-photo-probes, diff-check, and targeted import/privacy scans. Committed but not pushed.
